---
phase: 01-프로젝트-초기-설정
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .editorconfig
  - .gitignore
  - .nvmrc
  - .stylelintrc.cjs
  - scss/1-settings/_index.scss
  - scss/2-tools/_index.scss
  - scss/3-generic/_index.scss
  - scss/4-elements/_index.scss
  - scss/5-objects/_index.scss
  - scss/6-components/_index.scss
  - scss/7-utilities/_index.scss
autonomous: true
requirements:
  - SCSS-07
must_haves:
  truths:
    - "npm install 실행 시 bootstrap, sass, stylelint 등 devDependencies가 정상 설치된다"
    - "ITCSS 7개 레이어 폴더가 모두 존재하고 각각 _index.scss를 포함한다"
    - ".editorconfig에 2 spaces, UTF-8, LF 규칙이 명시되어 있다"
  artifacts:
    - path: "package.json"
      provides: "devDependencies, npm scripts"
      contains: "build:css"
    - path: "scss/1-settings/_index.scss"
      provides: "ITCSS layer 1 index"
    - path: "scss/7-utilities/_index.scss"
      provides: "ITCSS layer 7 index"
    - path: ".editorconfig"
      provides: "coding style rules"
      contains: "indent_size = 2"
  key_links: []
---

<objective>
프로젝트 골격을 생성한다: package.json 초기화, 의존성 설치, ITCSS 7-레이어 디렉토리 구조 생성, 에디터/린터/Git 설정 파일 배치.

Purpose: Phase 1의 모든 후속 작업(Bootstrap 통합, SCSS 컴파일)이 이 골격 위에서 동작한다.
Output: node_modules 설치 완료, ITCSS 폴더 구조, 설정 파일들
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-프로젝트-초기-설정/01-CONTEXT.md
@.planning/phases/01-프로젝트-초기-설정/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json 초기화 및 의존성 설치 + 설정 파일 생성</name>
  <files>package.json, .editorconfig, .gitignore, .nvmrc, .stylelintrc.cjs</files>
  <read_first>.planning/phases/01-프로젝트-초기-설정/01-RESEARCH.md</read_first>
  <action>
1. 프로젝트 루트에서 `npm init -y`로 package.json 생성 후 다음 필드를 수정한다:
   - "name": "artux"
   - "version": "0.1.0"
   - "description": "아트피큐 퍼블리싱 가이드 시스템"
   - "private": true
   - "scripts" 섹션에 CONTEXT.md 결정 사항 반영:
     ```json
     "build:css": "sass scss/style.scss dist/artux.css --style=compressed --source-map",
     "watch:css": "sass --watch scss/style.scss:dist/artux.css",
     "build": "npm run build:css",
     "lint:css": "stylelint \"scss/**/*.scss\""
     ```

2. devDependencies 설치 (01-RESEARCH.md Standard Stack 버전 사용):
   ```bash
   npm install -D bootstrap@5.3.8 sass@1.98.0 stylelint@17.5.0 stylelint-config-standard-scss@17.0.0 npm-run-all2@8.0.4
   npm install -D postcss@8.5.8 postcss-cli autoprefixer@10.4.27
   ```

3. `.editorconfig` 생성 (01-RESEARCH.md Code Examples 섹션 그대로):
   ```ini
   root = true

   [*]
   charset = utf-8
   end_of_line = lf
   indent_style = space
   indent_size = 2
   trim_trailing_whitespace = true
   insert_final_newline = true

   [*.{scss,css}]
   indent_size = 2

   [*.md]
   trim_trailing_whitespace = false
   ```

4. `.gitignore` 생성:
   ```
   node_modules/
   dist/
   *.css.map
   .DS_Store
   ```

5. `.nvmrc` 생성: 내용은 `24` (한 줄)

6. `.stylelintrc.cjs` 생성 (01-RESEARCH.md Code Examples 섹션 기반, Phase 1 warning 수준):
   ```javascript
   module.exports = {
     extends: ['stylelint-config-standard-scss'],
     rules: {
       // Phase 1: 경고만, 오류 없음 — 팀 적응 기간
       'scss/at-import-no-partial-leading-underscore': null,
       'scss/at-import-partial-extension': null,
     },
     ignoreFiles: ['node_modules/**', 'dist/**'],
   };
   ```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && node -e "const p=require('./package.json'); const ok = p.devDependencies.bootstrap && p.devDependencies.sass && p.scripts['build:css']; console.log(ok ? 'PASS' : 'FAIL')" && test -f .editorconfig && test -f .gitignore && test -f .nvmrc && test -f .stylelintrc.cjs && echo "ALL FILES EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - package.json에 "build:css" 스크립트가 존재한다
    - package.json devDependencies에 "bootstrap", "sass", "stylelint" 키가 존재한다
    - .editorconfig에 "indent_size = 2" 문자열이 존재한다
    - .gitignore에 "node_modules/" 문자열이 존재한다
    - .nvmrc에 "24" 문자열이 존재한다
    - .stylelintrc.cjs에 "stylelint-config-standard-scss" 문자열이 존재한다
  </acceptance_criteria>
  <done>package.json에 모든 devDependencies와 npm scripts가 정의되고, node_modules 설치 완료. 설정 파일 4종(.editorconfig, .gitignore, .nvmrc, .stylelintrc.cjs) 생성 완료.</done>
</task>

<task type="auto">
  <name>Task 2: ITCSS 7-레이어 디렉토리 구조 및 _index.scss 파일 생성</name>
  <files>scss/1-settings/_index.scss, scss/2-tools/_index.scss, scss/3-generic/_index.scss, scss/4-elements/_index.scss, scss/5-objects/_index.scss, scss/6-components/_index.scss, scss/7-utilities/_index.scss</files>
  <read_first>.planning/phases/01-프로젝트-초기-설정/01-CONTEXT.md</read_first>
  <action>
1. `scss/` 디렉토리와 7개 하위 레이어 폴더를 생성한다:
   ```
   scss/1-settings/
   scss/2-tools/
   scss/3-generic/
   scss/4-elements/
   scss/5-objects/
   scss/6-components/
   scss/7-utilities/
   ```

2. 각 레이어에 `_index.scss`를 생성한다. 한국어 주석으로 레이어 역할을 명시한다.
   빈 레이어도 반드시 _index.scss를 생성해야 한다 (없으면 style.scss @use 오류 발생).

   각 파일 내용:

   **scss/1-settings/_index.scss:**
   ```scss
   // ====================================================
   // 1-settings: 변수, 토큰, 브레이크포인트
   // CSS 출력 없음 — 설정값만 정의
   // ====================================================
   // @forward 'variables';  // Phase 1 Task(Plan 02)에서 활성화
   ```

   **scss/2-tools/_index.scss:**
   ```scss
   // ====================================================
   // 2-tools: 믹스인, 함수
   // CSS 출력 없음 — 도구만 정의
   // ====================================================
   // @forward 'mixins';  // Phase 2에서 추가
   ```

   **scss/3-generic/_index.scss:**
   ```scss
   // ====================================================
   // 3-generic: 외부 라이브러리(Bootstrap) + reset/normalize
   // 주의: _vendor.scss는 @import 기반이므로 @forward 불가
   // style.scss에서 별도 처리한다
   // ====================================================
   ```

   **scss/4-elements/_index.scss:**
   ```scss
   // ====================================================
   // 4-elements: HTML 태그 기본 스타일 (h1, a, p, html, body 등)
   // ====================================================
   // @forward 'base';  // Phase 1 Task(Plan 02)에서 활성화
   ```

   **scss/5-objects/_index.scss:**
   ```scss
   // ====================================================
   // 5-objects: 레이아웃 패턴 (container 확장, grid 등)
   // ====================================================
   // Phase 2 이후 채워짐
   ```

   **scss/6-components/_index.scss:**
   ```scss
   // ====================================================
   // 6-components: UI 컴포넌트 (btn, form, card 등)
   // ====================================================
   // Phase 6~9에서 채워짐
   ```

   **scss/7-utilities/_index.scss:**
   ```scss
   // ====================================================
   // 7-utilities: 유틸리티 클래스 (sr-only, visibility 등)
   // ====================================================
   // Phase 5에서 채워짐
   ```

3. `dist/` 디렉토리를 생성한다 (빌드 출력 대상, .gitignore에 포함됨).
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && for i in 1-settings 2-tools 3-generic 4-elements 5-objects 6-components 7-utilities; do test -f "scss/$i/_index.scss" || (echo "MISSING: scss/$i/_index.scss" && exit 1); done && echo "ALL 7 LAYERS OK"</automated>
  </verify>
  <acceptance_criteria>
    - scss/1-settings/_index.scss 파일이 존재하고 "1-settings" 문자열을 포함한다
    - scss/2-tools/_index.scss 파일이 존재하고 "2-tools" 문자열을 포함한다
    - scss/3-generic/_index.scss 파일이 존재하고 "3-generic" 문자열을 포함한다
    - scss/4-elements/_index.scss 파일이 존재하고 "4-elements" 문자열을 포함한다
    - scss/5-objects/_index.scss 파일이 존재하고 "5-objects" 문자열을 포함한다
    - scss/6-components/_index.scss 파일이 존재하고 "6-components" 문자열을 포함한다
    - scss/7-utilities/_index.scss 파일이 존재하고 "7-utilities" 문자열을 포함한다
    - 모든 _index.scss에 한국어 주석이 포함되어 있다
    - dist/ 디렉토리가 존재한다
  </acceptance_criteria>
  <done>ITCSS 7개 레이어 폴더에 각각 한국어 주석이 포함된 _index.scss가 존재한다. dist/ 디렉토리가 존재한다.</done>
</task>

</tasks>

<verification>
- `test -f package.json` -- package.json 존재
- `node -e "require('./package.json').devDependencies.bootstrap"` -- bootstrap 의존성 확인
- 7개 레이어 모두 `_index.scss` 존재 확인
- `.editorconfig`, `.gitignore`, `.nvmrc`, `.stylelintrc.cjs` 존재 확인
</verification>

<success_criteria>
- npm install이 정상 완료되어 node_modules/bootstrap/scss/ 경로가 존재한다
- ITCSS 7개 레이어 디렉토리와 _index.scss 파일이 모두 존재한다
- 설정 파일 4종이 올바른 내용으로 생성되었다
</success_criteria>

<output>
After completion, create `.planning/phases/01-프로젝트-초기-설정/01-01-SUMMARY.md`
</output>
