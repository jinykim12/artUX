---
phase: 02-scss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scss/1-settings/_variables.scss
  - scss/2-tools/_mixin.scss
  - scss/2-tools/_index.scss
  - scss/3-generic/_root.scss
  - scss/3-generic/_index.scss
autonomous: true
requirements: [SCSS-02, SCSS-03, SCSS-06]

must_haves:
  truths:
    - "_variables.scss에 Bootstrap 주요 변수 오버라이드 예시가 주석과 함께 문서화되어 있다"
    - "_mixin.scss에 팀 공통 믹스인 6종(focus, flex, ellipsis, position, drop-shadow, ani)이 사용 예시와 함께 정의되어 있다"
    - "_root.scss에 :root {} 뼈대가 존재하고 Phase 3 예정 토큰이 주석으로 안내되어 있다"
  artifacts:
    - path: "scss/1-settings/_variables.scss"
      provides: "Bootstrap 오버라이드 변수 문서화 원본"
      contains: "$font-size-base"
    - path: "scss/2-tools/_mixin.scss"
      provides: "팀 공통 믹스인 6종"
      contains: "@mixin focus"
    - path: "scss/2-tools/_index.scss"
      provides: "_mixin.scss @forward"
      contains: "@forward 'mixin'"
    - path: "scss/3-generic/_root.scss"
      provides: "CSS Custom Properties 뼈대"
      contains: ":root"
    - path: "scss/3-generic/_index.scss"
      provides: "_root.scss @forward"
      contains: "@forward 'root'"
  key_links:
    - from: "scss/2-tools/_index.scss"
      to: "scss/2-tools/_mixin.scss"
      via: "@forward 'mixin'"
      pattern: "@forward 'mixin'"
    - from: "scss/3-generic/_index.scss"
      to: "scss/3-generic/_root.scss"
      via: "@forward 'root'"
      pattern: "@forward 'root'"
---

<objective>
CSS 출력이 없는 설정/도구 레이어 파일 3종을 표준화한다: _variables.scss 확장 (Bootstrap 오버라이드 문서화 원본), _mixin.scss 신규 생성 (팀 믹스인 6종), _root.scss 뼈대 생성.

Purpose: 이 3종은 모두 CSS 출력을 생성하지 않는 레이어(1-settings, 2-tools, 3-generic 뼈대)이므로, 빌드 실패 위험 없이 독립적으로 작업할 수 있다.
Output: _variables.scss(확장), _mixin.scss(신규), _root.scss(신규), 관련 _index.scss 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scss/02-RESEARCH.md
@.planning/phases/01-프로젝트-초기-설정/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Phase 1에서 확립된 현재 파일 구조 -->

From scss/1-settings/_variables.scss (현재 상태):
```scss
$font-size-base: 1.6rem !default; // 16px (1rem = 10px 환경)
```

From scss/2-tools/_index.scss (현재 상태):
```scss
// @forward 'mixins';  // Phase 2에서 추가
```

From scss/3-generic/_index.scss (현재 상태 — 빈 파일, 주석만):
```scss
// 주의: _vendor.scss는 @import 기반이므로 @forward 불가
```

From scss/3-generic/_vendor.scss (Bootstrap 오버라이드 위치):
```scss
$font-size-base: 1.6rem;
@import "bootstrap/scss/bootstrap";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: _variables.scss 확장 + _root.scss 뼈대 + _index.scss 업데이트</name>
  <files>scss/1-settings/_variables.scss, scss/3-generic/_root.scss, scss/3-generic/_index.scss</files>
  <action>
**1-1. `scss/1-settings/_variables.scss` 전체 재작성 (per SCSS-02)**

기존 내용을 아래로 교체한다. 이 파일은 Bootstrap 오버라이드 변수의 "문서화 원본" 역할이다. 실제 Bootstrap !default 오버라이드 효력은 `3-generic/_vendor.scss` 내 @import 스코프에서만 발생한다. `!default` 플래그를 제거하고 값만 기록한다.

```scss
// ====================================================
// artpqUX Bootstrap 오버라이드 변수 — 문서화 원본
// ====================================================
//
// [중요] 실제 Bootstrap 오버라이드 효력 위치:
//   scss/3-generic/_vendor.scss 내 @import 스코프
//
// 이 파일은 팀이 "어떤 값을 오버라이드하는지" 확인하는
// 문서 역할을 한다. 값을 변경할 때는 반드시
// _vendor.scss도 동일하게 업데이트할 것.
//
// ====================================================

// ----------------------------------------------------
// 62.5% REM 트릭 대응
// html { font-size: 62.5% } → 1rem = 10px
// Bootstrap 기본 1rem = 16px 유지를 위해 1.6rem으로 재설정
// ----------------------------------------------------
$font-size-base: 1.6rem; // 16px

// ----------------------------------------------------
// 색상 오버라이드 예시 — Phase 3에서 실제값 결정
// 아래 주석을 해제하고 _vendor.scss에도 동일하게 선언할 것
// ----------------------------------------------------
// $primary: #0d6efd;       // Bootstrap 기본값 유지 예시
// $secondary: #6c757d;     // Bootstrap 기본값 유지 예시

// ----------------------------------------------------
// 간격 오버라이드 예시
// $spacer는 Bootstrap 기본 1rem (= 10px in 62.5% 환경)
// 필요 시 $spacer: 1.6rem으로 16px 기준 유지
// ----------------------------------------------------
// $spacer: 1rem;

// ----------------------------------------------------
// 기타 오버라이드 예시
// ----------------------------------------------------
// $border-radius: 0.4rem;  // 4px (기본 0.375rem = 3.75px)
// $font-family-base: "Pretendard GOV", sans-serif;
```

**1-2. `scss/3-generic/_root.scss` 신규 생성 (per SCSS-06)**

```scss
// ====================================================
// artpqUX CSS Custom Properties (디자인 토큰)
//
// Phase 2: 뼈대만 생성 — 토큰은 Phase 3에서 정의
// Phase 3 예정 토큰:
//   --color-primary, --color-secondary, --color-text,
//   --color-border, --color-error, --color-success
//   --font-size-xs ~ --font-size-2xl
//   --spacing-xs(4px) ~ --spacing-3xl(64px)
//   --shadow-sm, --shadow-md, --shadow-lg
//   --transition-fast(150ms), --transition-base(300ms)
//   --z-modal(1050), --z-overlay(1040)
// ====================================================

:root {
  // 토큰은 Phase 3에서 채워진다
}
```

**1-3. `scss/3-generic/_index.scss` 업데이트**

기존 주석을 유지하면서 `@forward 'root'`를 추가한다. `_vendor.scss`는 여전히 `@forward` 불가 — `@import` 기반이므로 `style.scss`에서 별도 처리.

```scss
// ====================================================
// 3-generic: CSS Custom Properties + 외부 라이브러리(Bootstrap)
// 주의: _vendor.scss는 @import 기반이므로 @forward 불가
// style.scss에서 @import '3-generic/vendor'로 별도 처리한다
// ====================================================
@forward 'root';
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && grep -c '$font-size-base' scss/1-settings/_variables.scss && grep -c '_vendor.scss' scss/1-settings/_variables.scss && test -f scss/3-generic/_root.scss && grep ':root' scss/3-generic/_root.scss && grep "@forward 'root'" scss/3-generic/_index.scss && echo "PASS"</automated>
  </verify>
  <done>
- _variables.scss에 $font-size-base 실제값 + 색상/간격/기타 오버라이드 예시가 주석으로 포함
- _variables.scss 상단에 "문서화 원본" 역할과 _vendor.scss 동기화 필수 안내가 명시
- _root.scss에 :root {} 뼈대 + Phase 3 예정 토큰 주석이 존재
- 3-generic/_index.scss에 @forward 'root'가 활성화
  </done>
</task>

<task type="auto">
  <name>Task 2: _mixin.scss 생성 + 2-tools/_index.scss 활성화</name>
  <files>scss/2-tools/_mixin.scss, scss/2-tools/_index.scss</files>
  <action>
**2-1. `scss/2-tools/_mixin.scss` 신규 생성 (per SCSS-03)**

팀 공통 믹스인 6종을 Dart Sass 1.x 호환으로 작성한다. Autoprefixer가 벤더 프리픽스를 처리하므로 `-webkit-`, `-moz-`, `-ms-`, `-o-` 수동 작성은 하지 않는다. 단, `-webkit-line-clamp`과 `-webkit-box-orient`는 Autoprefixer가 처리하지 않으므로 수동 유지.

믹스인 기본값에서 Bootstrap 변수($primary 등)를 참조하지 않는다 — 2-tools 레이어는 Bootstrap 로드 이전이므로 undefined variable 오류가 발생한다. 하드코딩된 기본값을 사용한다.

```scss
// ====================================================
// artpqUX 팀 공통 믹스인
// Dart Sass 1.x 호환. Autoprefixer가 벤더 프리픽스를 처리하므로
// -webkit-, -moz-, -ms-, -o- 수동 작성 불필요.
// 예외: -webkit-line-clamp, -webkit-box-orient는 수동 유지.
// ====================================================

// ----------------------------------------------------
// [포커스 스타일]
// 사용 예: @include focus();
//          @include focus($color: #007bff, $alpha: 0.6);
// 파라미터:
//   $offset: outline-offset 값 (기본 -0.4rem)
//   $color:  outline 색상 (기본 #0d6efd — Bootstrap $primary 기본값)
//   $alpha:  불투명도 0~1 (기본 0.8)
//
// 주의: outline: none 사용 금지 — 공공기관 접근성 위반
// ----------------------------------------------------
@mixin focus($offset: -0.4rem, $color: #0d6efd, $alpha: 0.8) {
  outline-offset: $offset;
  outline: 2px solid rgba($color, $alpha);
}

// ----------------------------------------------------
// [flex 중앙정렬]
// 사용 예: @include flex();
// 수평+수직 중앙 정렬 shorthand
// ----------------------------------------------------
@mixin flex {
  display: flex;
  align-items: center;
  justify-content: center;
}

// ----------------------------------------------------
// [여러 줄 말줄임]
// 사용 예: @include ellipsis(2); // 2줄 말줄임
// 파라미터: $line — 표시할 최대 줄 수
// -webkit-line-clamp은 Autoprefixer가 처리하지 않으므로 수동 유지
// ----------------------------------------------------
@mixin ellipsis($line) {
  display: -webkit-box;
  overflow: hidden;
  text-overflow: ellipsis;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: $line;
}

// ----------------------------------------------------
// [포지션 단축]
// 사용 예: @include position(absolute, $t: 0, $r: 0);
//          @include position(fixed, $b: 2rem, $l: 50%);
// 파라미터: $p=position 속성값, $t=top, $b=bottom, $l=left, $r=right
// ----------------------------------------------------
@mixin position($p: absolute, $t: null, $b: null, $l: null, $r: null) {
  position: $p;
  top: $t;
  bottom: $b;
  left: $l;
  right: $r;
}

// ----------------------------------------------------
// [박스 그림자]
// 사용 예: @include drop-shadow();
//          @include drop-shadow($x: 2px, $y: 4px, $blur: 8px, $alpha: 0.15);
// 파라미터: $x=가로 오프셋, $y=세로 오프셋, $blur=흐림반경,
//           $spread=확산, $alpha=불투명도
// ----------------------------------------------------
@mixin drop-shadow($x: 0, $y: 2px, $blur: 2px, $spread: 0, $alpha: 0.25) {
  box-shadow: $x $y $blur $spread rgba(0, 0, 0, $alpha);
}

// ----------------------------------------------------
// [전환 애니메이션]
// 사용 예: @include ani();         // 0.4s linear 전환
//          @include ani(0.2s);     // 0.2s linear 전환
// 파라미터: $time — 전환 시간 (기본 0.4s)
// ----------------------------------------------------
@mixin ani($time: 0.4s) {
  transition: all $time linear;
}
```

**2-2. `scss/2-tools/_index.scss` 업데이트**

주석 처리된 `@forward 'mixins'`를 활성화하되, 파일명은 `mixin` (s 없이)으로 수정한다.

```scss
// ====================================================
// 2-tools: 믹스인, 함수
// CSS 출력 없음 — 도구만 정의
// ====================================================
@forward 'mixin';
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && test -f scss/2-tools/_mixin.scss && grep -c '@mixin focus' scss/2-tools/_mixin.scss && grep -c '@mixin flex' scss/2-tools/_mixin.scss && grep -c '@mixin ellipsis' scss/2-tools/_mixin.scss && grep -c '@mixin position' scss/2-tools/_mixin.scss && grep -c '@mixin drop-shadow' scss/2-tools/_mixin.scss && grep -c '@mixin ani' scss/2-tools/_mixin.scss && grep "@forward 'mixin'" scss/2-tools/_index.scss && echo "PASS"</automated>
  </verify>
  <done>
- _mixin.scss에 6종 믹스인(focus, flex, ellipsis, position, drop-shadow, ani)이 모두 정의됨
- 각 믹스인에 한국어 주석으로 사용법, 파라미터 설명, 사용 예시가 포함됨
- 벤더 프리픽스가 Autoprefixer 대상인 항목은 제거됨 (-webkit-line-clamp, -webkit-box-orient만 유지)
- focus 믹스인에 outline: none 사용 금지 경고 주석이 포함됨
- 2-tools/_index.scss에 @forward 'mixin'이 활성화됨
  </done>
</task>

</tasks>

<verification>
Wave 1 완료 후 검증 (Plan 02 실행 전):

1. 파일 존재 확인:
```bash
ls -la scss/1-settings/_variables.scss scss/2-tools/_mixin.scss scss/2-tools/_index.scss scss/3-generic/_root.scss scss/3-generic/_index.scss
```

2. _variables.scss에 문서화 원본 안내 포함:
```bash
grep '문서화 원본' scss/1-settings/_variables.scss
```

3. _mixin.scss에 6종 믹스인 존재:
```bash
grep -c '@mixin' scss/2-tools/_mixin.scss  # 6 이상
```

4. _root.scss에 :root 블록 존재:
```bash
grep ':root' scss/3-generic/_root.scss
```

5. outline: none 패턴 없음 확인:
```bash
grep -r 'outline.*none' scss/2-tools/ && echo "FAIL: outline: none found" || echo "PASS"
```
</verification>

<success_criteria>
1. _variables.scss에 $font-size-base 실제 선언 + 색상/간격/border-radius/font-family 오버라이드 예시가 주석으로 포함
2. _variables.scss 상단에 _vendor.scss 동기화 필수 안내가 명시
3. _mixin.scss에 6종 믹스인이 Dart Sass 호환으로 정의되고, 각 믹스인에 사용 예시 주석 포함
4. _mixin.scss에 outline: none 패턴이 없음
5. _root.scss에 :root {} 뼈대가 존재하고 Phase 3 예정 토큰 목록이 주석으로 안내
6. 2-tools/_index.scss에 @forward 'mixin' 활성화
7. 3-generic/_index.scss에 @forward 'root' 활성화
</success_criteria>

<output>
After completion, create `.planning/phases/02-scss/02-01-SUMMARY.md`
</output>
