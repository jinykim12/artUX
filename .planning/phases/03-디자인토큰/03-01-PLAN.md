---
phase: 03-디자인토큰
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scss/3-generic/_root.scss
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04

must_haves:
  truths:
    - "dist/artux.css의 :root 블록에 --color-primary 등 8종 색상 토큰이 출력된다"
    - "dist/artux.css의 :root 블록에 --font-size-xs ~ --font-size-2xl 7단계 타이포 토큰이 출력된다"
    - "dist/artux.css의 :root 블록에 --spacing-xs ~ --spacing-3xl 7단계 간격 토큰이 4px 배수 rem값으로 출력된다"
    - "dist/artux.css의 :root 블록에 shadow 3종, transition 2종, z-index 3종 토큰이 출력된다"
  artifacts:
    - path: "scss/3-generic/_root.scss"
      provides: "CSS Custom Properties 디자인 토큰 4종(색상, 타이포, 간격, 기타)"
      contains: "--color-primary"
  key_links:
    - from: "scss/3-generic/_root.scss"
      to: "dist/artux.css"
      via: "npm run build:css (Dart Sass 컴파일)"
      pattern: "--color-primary.*#0d6efd"
---

<objective>
_root.scss의 빈 :root {} 블록을 4개 범주(색상, 타이포그래피, 간격, 기타)의 CSS Custom Properties 토큰으로 채운다.

Purpose: 이후 컴포넌트 Phase에서 var(--color-primary), var(--spacing-md) 등으로 참조할 수 있는 단일 토큰 원본을 확립한다.
Output: scss/3-generic/_root.scss (토큰 실체 포함)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-디자인토큰/03-RESEARCH.md
@scss/3-generic/_root.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: _root.scss에 4종 디자인 토큰 선언</name>
  <files>scss/3-generic/_root.scss</files>
  <action>
현재 빈 :root {} 블록을 아래 내용으로 교체한다. 파일 상단 주석은 Phase 3 완성본으로 갱신한다.

파일 전체 내용:

```scss
// ====================================================
// artpqUX CSS Custom Properties (디자인 토큰)
//
// 토큰 네이밍 규칙: --[카테고리]-[이름]-[변형]
//   --color-*       색상
//   --font-size-*   폰트 크기
//   --font-weight-* 폰트 굵기
//   --leading-*     줄간격
//   --spacing-*     간격
//   --shadow-*      그림자
//   --transition-*  전환 속도
//   --z-*           z-index
//
// Bootstrap 충돌 방지:
//   Bootstrap 5.3은 --bs-* 접두어 사용 → 팀 토큰과 이름 충돌 없음
//
// [62.5% REM 환경] html { font-size: 62.5% } → 1rem = 10px
//   px→rem 환산: px / 10 = rem (예: 16px = 1.6rem)
// ====================================================

:root {
  // ── 색상 토큰 (TOKEN-01) ──────────────────────────
  // Bootstrap Sass 변수 ↔ CSS Custom Properties 매핑:
  //   $primary (#0d6efd)        → --color-primary
  //   $secondary (#6c757d)      → --color-secondary
  //   $body-color (#212529)     → --color-text
  //   $text-muted (#6c757d)     → --color-text-muted
  //   $body-bg (#fff)           → --color-bg
  //   $border-color (#dee2e6)   → --color-border
  //   $danger (#dc3545)         → --color-error
  //   $success (#198754)        → --color-success
  //
  // [중요] $primary: var(--color-primary) 사용 불가
  // Sass 변수는 컴파일-타임 정적값이어야 한다.
  // 값 변경 시 _vendor.scss의 $primary도 동일하게 수정할 것.
  --color-primary:    #0d6efd;
  --color-secondary:  #6c757d;
  --color-text:       #212529;
  --color-text-muted: #6c757d;
  --color-bg:         #fff;
  --color-border:     #dee2e6;
  --color-error:      #dc3545;
  --color-success:    #198754;

  // ── 타이포그래피 토큰 (TOKEN-02) ──────────────────
  // [62.5% REM] 1rem = 10px
  --font-size-xs:   1.0rem;  // 10px
  --font-size-sm:   1.2rem;  // 12px
  --font-size-base: 1.6rem;  // 16px — Bootstrap $font-size-base 대응
  --font-size-md:   1.8rem;  // 18px
  --font-size-lg:   2.0rem;  // 20px
  --font-size-xl:   2.4rem;  // 24px
  --font-size-2xl:  3.2rem;  // 32px

  --font-weight-regular: 400;
  --font-weight-medium:  500;
  --font-weight-bold:    700;

  --leading-tight:  1.25;
  --leading-normal: 1.5;   // Bootstrap $line-height-base 기본값
  --leading-loose:  1.75;

  // ── 간격 토큰 — 4px 기반 7단계 (TOKEN-03) ────────
  // [62.5% REM] 4px = 0.4rem
  --spacing-xs:  0.4rem;  //  4px
  --spacing-sm:  0.8rem;  //  8px
  --spacing-md:  1.6rem;  // 16px
  --spacing-lg:  2.4rem;  // 24px
  --spacing-xl:  3.2rem;  // 32px
  --spacing-2xl: 4.8rem;  // 48px
  --spacing-3xl: 6.4rem;  // 64px

  // ── 기타 토큰 (TOKEN-04) ─────────────────────────
  // 그림자 — Bootstrap --bs-box-shadow-* 값 참조
  --shadow-sm: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
  --shadow-md: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
  --shadow-lg: 0 1rem 3rem rgba(0, 0, 0, 0.175);

  // 전환
  --transition-fast: 150ms ease;
  --transition-base: 300ms ease;

  // z-index — Bootstrap z-index 체계와 정렬
  --z-dropdown: 1000;
  --z-overlay:  1040;
  --z-modal:    1050;
}
```

주의사항:
- 모든 rem 값은 62.5% REM 환경 기준 (1rem=10px). 16px=1.6rem이 아니라 16px=1.6rem으로 맞다.
- 색상값은 Bootstrap 5.3 기본 팔레트 기준이다. 프로젝트별 색상은 _project-overrides.scss에서 오버라이드한다 (Plan 02에서 생성).
- --color-text-muted 포함 필수 (연구 결과에서 확정).
- --font-size-md(18px) 포함 — 공공기관 UI에서 빈용됨.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && npm run build:css 2>&1 | tail -3 && echo "EXIT:$?" && echo "--- color tokens ---" && grep -c '\-\-color-' dist/artux.css && echo "--- font-size tokens ---" && grep -c '\-\-font-size-' dist/artux.css && echo "--- spacing tokens ---" && grep -c '\-\-spacing-' dist/artux.css && echo "--- shadow tokens ---" && grep -c '\-\-shadow-' dist/artux.css && echo "--- transition tokens ---" && grep -c '\-\-transition-' dist/artux.css && echo "--- z-index tokens ---" && grep -c '\-\-z-' dist/artux.css</automated>
  </verify>
  <done>
    - npm run build:css가 exit 0으로 성공한다
    - dist/artux.css에 --color-* 토큰 8종 이상 출력된다
    - dist/artux.css에 --font-size-* 토큰 7종 이상 출력된다
    - dist/artux.css에 --spacing-* 토큰 7종 출력된다
    - dist/artux.css에 --shadow-*, --transition-*, --z-* 토큰이 각각 출력된다
    - 모든 간격 토큰이 4px 배수의 rem 값이다 (0.4, 0.8, 1.6, 2.4, 3.2, 4.8, 6.4)
  </done>
</task>

</tasks>

<verification>
빌드 후 dist/artux.css의 :root 블록에서 4종 범주 토큰 존재 여부를 grep으로 확인:
```bash
cd /Users/johyeonchang/Desktop/artpqUX
npm run build:css
grep -E '\-\-color-(primary|secondary|text|bg|border|error|success)' dist/artux.css | head -10
grep -E '\-\-font-size-(xs|sm|base|md|lg|xl|2xl)' dist/artux.css | head -10
grep -E '\-\-spacing-(xs|sm|md|lg|xl|2xl|3xl)' dist/artux.css | head -10
grep -E '\-\-(shadow|transition|z)-' dist/artux.css | head -10
```
</verification>

<success_criteria>
1. npm run build:css가 오류 없이 완료된다
2. dist/artux.css의 :root 블록에 색상 8종, 타이포 13종(font-size 7 + font-weight 3 + leading 3), 간격 7종, 기타 8종(shadow 3 + transition 2 + z-index 3) 토큰이 모두 포함된다
3. --spacing-xs(0.4rem)부터 --spacing-3xl(6.4rem)까지 7단계가 4px 배수 rem 값으로 정의된다
</success_criteria>

<output>
After completion, create `.planning/phases/03-디자인토큰/03-01-SUMMARY.md`
</output>
