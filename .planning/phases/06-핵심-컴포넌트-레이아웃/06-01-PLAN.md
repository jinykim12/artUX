---
phase: 06-핵심-컴포넌트-레이아웃
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scss/5-objects/_layout.scss
  - scss/5-objects/_index.scss
  - scss/6-components/_header.scss
  - scss/6-components/_index.scss
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "_layout.scss가 Bootstrap 그리드 사용 원칙 주석만 포함하고 실제 CSS 규칙은 없다"
    - "_header.scss가 respond-to(pc-sm) 믹스인으로 PC/모바일 헤더 스타일을 분기한다"
    - "SCSS 컴파일이 오류 없이 완료된다"
  artifacts:
    - path: "scss/5-objects/_layout.scss"
      provides: "Bootstrap 그리드 사용 원칙 주석"
      contains: "중복 금지"
    - path: "scss/6-components/_header.scss"
      provides: "헤더 공통 스타일 + respond-to() 분기"
      contains: "respond-to(pc-sm)"
    - path: "scss/5-objects/_index.scss"
      provides: "@forward 'layout' 추가"
      contains: "@forward 'layout'"
    - path: "scss/6-components/_index.scss"
      provides: "@forward 'header' 추가"
      contains: "@forward 'header'"
  key_links:
    - from: "scss/6-components/_header.scss"
      to: "scss/2-tools/_breakpoints.scss"
      via: "@use respond-to()"
      pattern: "respond-to\\(pc-sm\\)"
    - from: "scss/5-objects/_index.scss"
      to: "scss/5-objects/_layout.scss"
      via: "@forward"
      pattern: "@forward 'layout'"
    - from: "scss/6-components/_index.scss"
      to: "scss/6-components/_header.scss"
      via: "@forward"
      pattern: "@forward 'header'"
---

<objective>
헤더/GNB SCSS 컴포넌트와 레이아웃 원칙 파일을 생성하여, artpqUX 팀 표준 헤더 스타일 인프라를 구축한다.

Purpose: PC/모바일 헤더 레이아웃 스타일이 respond-to() 믹스인으로 분기되고, Bootstrap 그리드 중복 금지 원칙이 명시되어야 한다.
Output: `_layout.scss` (주석 전용), `_header.scss` (헤더 스타일), 두 _index.scss 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-핵심-컴포넌트-레이아웃/06-CONTEXT.md
@.planning/phases/06-핵심-컴포넌트-레이아웃/06-RESEARCH.md

@scss/5-objects/_index.scss
@scss/6-components/_index.scss
@scss/6-components/_skip-nav.scss
@scss/2-tools/_breakpoints.scss
@scss/3-generic/_root.scss

<interfaces>
<!-- respond-to() 믹스인 키워드 (scss/2-tools/_breakpoints.scss) -->
<!-- 사용 가능 키워드: tablet, pc-sm, pc, mobile-only, tablet-only, pc-sm-only -->
<!-- 모바일 퍼스트: 기본 스타일 = 모바일, respond-to(pc-sm) 이상에서 PC 확장 -->

From scss/3-generic/_root.scss — 헤더에서 사용할 토큰:
```scss
--color-bg:         #fff;
--color-border:     #dee2e6;
--color-primary:    #0d6efd;
--spacing-md:  1.6rem;  // 16px
--spacing-lg:  2.4rem;  // 24px
--z-dropdown: 1000;
```

From scss/6-components/_skip-nav.scss — 기존 컴포넌트 패턴:
- 한국어 주석 블록 (사용법, 주의사항)
- BEM 네이밍 아닌 단일 클래스 + 상태 셀렉터 패턴
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _layout.scss 생성 + 5-objects _index.scss 업데이트</name>
  <files>scss/5-objects/_layout.scss, scss/5-objects/_index.scss</files>
  <read_first>
    - scss/5-objects/_index.scss
    - scss/6-components/_skip-nav.scss
    - .planning/phases/06-핵심-컴포넌트-레이아웃/06-RESEARCH.md
  </read_first>
  <action>
1. `scss/5-objects/_layout.scss` 신규 생성 — per D-05, D-06:
   - 실제 CSS 규칙 없이 주석만으로 구성
   - 06-RESEARCH.md의 "Pattern 3: `_layout.scss` — 주석 전용 파일" 코드 블록을 기반으로 작성
   - 반드시 포함할 주석 내용:
     - 제목 블록: `[레이아웃 원칙 — 5-objects]`
     - Bootstrap 5 그리드 시스템 활용 원칙
     - 컨테이너 클래스 목록: `.container`, `.container-fluid`, `.container-{bp}`
     - 그리드 클래스 목록: `.row`, `.col`, `.col-{n}`, `.col-{bp}-{n}`, `.g-{n}`, `.gx-{n}`
     - 중복 금지 원칙 (D-06): Bootstrap 그리드 클래스를 SCSS로 재정의/복제 금지
     - 커스텀 확장 필요 시 Bootstrap 유틸리티/오버라이드 우선 검토 안내
   - `_skip-nav.scss`의 한국어 주석 스타일을 따름 (===== 구분선, 블록 주석)

2. `scss/5-objects/_index.scss` 업데이트:
   - 기존 `// Phase 2 이후 채워짐` 주석 제거
   - `@forward 'layout';` 추가 + 한국어 주석 `// Bootstrap 그리드 사용 원칙 주석 (COMP-01)`
  </action>
  <verify>
    <automated>npx sass scss/style.scss /dev/null --no-source-map 2>&1 && grep -c "중복 금지" scss/5-objects/_layout.scss && grep "@forward 'layout'" scss/5-objects/_index.scss</automated>
  </verify>
  <acceptance_criteria>
    - `grep "레이아웃 원칙" scss/5-objects/_layout.scss` 출력 있음
    - `grep "중복 금지" scss/5-objects/_layout.scss` 출력 있음
    - `grep "container" scss/5-objects/_layout.scss` 출력 있음
    - `grep "@forward 'layout'" scss/5-objects/_index.scss` 출력 있음
    - `npx sass scss/style.scss /dev/null --no-source-map` 오류 없이 완료
    - `scss/5-objects/_layout.scss`에 CSS 규칙(중괄호 `{`)이 없음: `grep -c "{" scss/5-objects/_layout.scss` 결과 0
  </acceptance_criteria>
  <done>_layout.scss가 Bootstrap 그리드 주석 전용 파일로 존재하고, 5-objects/_index.scss에서 @forward되며, SCSS 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: _header.scss 생성 + 6-components _index.scss 업데이트</name>
  <files>scss/6-components/_header.scss, scss/6-components/_index.scss</files>
  <read_first>
    - scss/6-components/_index.scss
    - scss/6-components/_skip-nav.scss
    - scss/2-tools/_breakpoints.scss
    - scss/3-generic/_root.scss
    - .planning/phases/06-핵심-컴포넌트-레이아웃/06-RESEARCH.md
  </read_first>
  <action>
1. `scss/6-components/_header.scss` 신규 생성 — per D-10:
   - 06-RESEARCH.md의 "Pattern 4: `_header.scss` — respond-to() 분기 구조" 코드 블록을 기반으로 작성
   - `@use '../2-tools/breakpoints' as *;` 로 respond-to() 믹스인 사용
   - 한국어 주석 블록 포함 (skip-nav.scss 패턴 계승):
     - 제목: `[헤더 / GNB — COMP-01]`
     - PC: respond-to(pc-sm) 이상 GNB 수평 표시
     - 모바일: 햄버거 버튼 + 전체메뉴 패널
     - 사용 예 HTML: `<header class="header" role="banner">` 구조
     - 접근성 주의사항: aria-label, aria-expanded, aria-hidden 필수 항목 명시
   - 구현할 셀렉터 (모바일 퍼스트):
     - `.header` — sticky, top:0, z-index: var(--z-dropdown), background: var(--color-bg), border-bottom
     - `.header__inner` — flex, align-items:center, justify-content:space-between, height:6.0rem(모바일) → 8.0rem(PC respond-to(pc-sm))
     - `.header__gnb` — display:none 기본 → respond-to(pc-sm) display:flex
     - `.header__hamburger` — 모바일에서만 표시, respond-to(pc-sm) display:none
     - `.header__mobile-menu` — display:none 기본, `&[aria-hidden="false"]` display:block, respond-to(pc-sm) display:none !important
   - GNB 서브메뉴 스타일:
     - `.gnb` — PC에서 수평 flex 레이아웃
     - `.gnb__item` — 상대 position (서브메뉴 절대 배치 기준)
     - `.gnb__toggle` — 서브메뉴 토글 버튼 스타일
     - `.gnb__sub` — 서브메뉴 목록, hidden 속성으로 숨김, `&:not([hidden])` 표시
     - `.gnb__link` — 메뉴 링크 + `[aria-current="page"]` 현재 페이지 스타일 (D-08)
   - 서브메뉴 드롭다운에 트랜지션 없음 (공공기관 즉시 표시 원칙)
   - CSS 토큰만 사용: var(--color-*), var(--spacing-*), var(--z-*), var(--font-size-*)

2. `scss/6-components/_index.scss` 업데이트 — per D-11:
   - 기존 `@forward 'skip-nav';` 아래에 `@forward 'header';` 추가
   - 주석: `// 헤더/GNB (COMP-01)`
   - `// Phase 6~9에서 추가 컴포넌트 채워짐` 주석 유지
  </action>
  <verify>
    <automated>npx sass scss/style.scss /dev/null --no-source-map 2>&1 && grep -c "respond-to(pc-sm)" scss/6-components/_header.scss && grep "@forward 'header'" scss/6-components/_index.scss</automated>
  </verify>
  <acceptance_criteria>
    - `npx sass scss/style.scss /dev/null --no-source-map` 오류 없이 완료
    - `grep "respond-to(pc-sm)" scss/6-components/_header.scss` 출력 있음
    - `grep "header__mobile-menu" scss/6-components/_header.scss` 출력 있음
    - `grep "aria-hidden" scss/6-components/_header.scss` 출력 있음
    - `grep "aria-current" scss/6-components/_header.scss` 출력 있음
    - `grep "z-dropdown" scss/6-components/_header.scss` 출력 있음
    - `grep "gnb__sub" scss/6-components/_header.scss` 출력 있음
    - `grep "@forward 'header'" scss/6-components/_index.scss` 출력 있음
    - `grep "@forward 'skip-nav'" scss/6-components/_index.scss` 출력 있음 (기존 유지 확인)
  </acceptance_criteria>
  <done>_header.scss가 respond-to(pc-sm) 분기로 PC/모바일 헤더 스타일을 구현하고, 6-components/_index.scss에서 @forward되며, SCSS 컴파일 성공</done>
</task>

</tasks>

<verification>
- `npx sass scss/style.scss /dev/null --no-source-map` 오류 없이 완료
- `scss/5-objects/_layout.scss` 존재하고 CSS 규칙 없음 (주석 전용)
- `scss/6-components/_header.scss` 존재하고 respond-to(pc-sm) 포함
- 두 _index.scss 파일에 각각 @forward 추가 확인
</verification>

<success_criteria>
1. SCSS 컴파일 성공 (빌드 오류 없음)
2. _layout.scss는 주석만 포함, CSS 규칙 없음
3. _header.scss는 모바일 퍼스트 + respond-to(pc-sm) PC 분기 구현
4. 헤더 스타일이 CSS 토큰(var(--*))만 사용
5. _index.scss 파일에 @forward 추가 완료
</success_criteria>

<output>
After completion, create `.planning/phases/06-핵심-컴포넌트-레이아웃/06-01-SUMMARY.md`
</output>
