---
phase: 06-핵심-컴포넌트-레이아웃
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/components/header.md
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "PC GNB HTML 스니펫이 WAI-ARIA Disclosure Navigation 패턴으로 작성되어 있다"
    - "모바일 전체메뉴 HTML 스니펫이 aria-hidden 토글 방식으로 작성되어 있다"
    - "GNB 접근성 JS 예시 코드가 포함되어 있다"
    - "aria-current='page' 현재 페이지 표시 패턴이 포함되어 있다"
  artifacts:
    - path: "docs/components/header.md"
      provides: "PC GNB + 모바일 전체메뉴 HTML 스니펫 + JS 예시"
      contains: "aria-expanded"
      min_lines: 100
  key_links:
    - from: "docs/components/header.md"
      to: "scss/6-components/_header.scss"
      via: "CSS 클래스명 일치"
      pattern: "header__"
---

<objective>
헤더/GNB 마크업 패턴 문서를 작성하여, PC 메뉴와 모바일 전체메뉴의 접근성 포함 HTML 스니펫과 JS 예시를 팀에 제공한다.

Purpose: 프로젝트마다 반복되는 GNB 마크업을 표준화하고, WAI-ARIA Disclosure Navigation 패턴과 접근성 JS 코드를 참고 가능하게 한다.
Output: `docs/components/header.md` (PC + 모바일 통합 문서)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-핵심-컴포넌트-레이아웃/06-CONTEXT.md
@.planning/phases/06-핵심-컴포넌트-레이아웃/06-RESEARCH.md

@docs/accessibility/keyboard.md
@scss/6-components/_skip-nav.scss

<interfaces>
<!-- 06-RESEARCH.md에서 확정된 HTML 마크업 패턴 -->
<!-- PC GNB: Disclosure Navigation — <ul class="gnb"> + <button aria-expanded> + <ul class="gnb__sub"> -->
<!-- 모바일: <button class="header__hamburger" aria-expanded aria-controls="mobile-menu"> + <div id="mobile-menu" aria-hidden> -->
<!-- CSS 클래스 (06-01-PLAN에서 _header.scss가 생성): header, header__inner, header__logo, header__gnb, header__hamburger, header__mobile-menu, gnb, gnb__item, gnb__toggle, gnb__sub, gnb__link -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/components/header.md 작성</name>
  <files>docs/components/header.md</files>
  <read_first>
    - .planning/phases/06-핵심-컴포넌트-레이아웃/06-RESEARCH.md
    - .planning/phases/06-핵심-컴포넌트-레이아웃/06-CONTEXT.md
    - docs/accessibility/keyboard.md
    - scss/6-components/_skip-nav.scss
  </read_first>
  <action>
1. `docs/components/` 디렉토리 생성 (존재하지 않음)

2. `docs/components/header.md` 신규 생성 — per D-01, D-03:
   PC GNB와 모바일 전체메뉴를 하나의 마크다운 문서에 함께 작성한다.
   Phase 5 docs/ 마크다운 패턴과 일관성 유지.

   **문서 구조:**

   ```
   # 헤더 / GNB (COMP-01)
   > 개요 설명

   ## 기본 구조
   - 헤더 전체 HTML 구조 설명 (header > skip-nav + logo + gnb + hamburger + mobile-menu)

   ## PC GNB 마크업 (per D-02)
   - WAI-ARIA Disclosure Navigation 패턴 코드 블록
   - 2단계 서브메뉴(드롭다운) 포함
   - `<nav aria-label="주 메뉴">` + `<ul class="gnb">`
   - 서브메뉴: `<button aria-expanded="false" aria-controls="submenu-{id}">` + `<ul id="submenu-{id}" class="gnb__sub" hidden>`
   - 단순 링크: `<a href="..." class="gnb__link">`
   - role="menu" 사용 금지 경고 주석
   - aria-haspopup 사용 금지 (Disclosure 패턴에서는 불필요)

   ## 모바일 전체메뉴 마크업 (per D-04)
   - 햄버거 버튼: `<button class="header__hamburger" aria-expanded="false" aria-controls="mobile-menu" aria-label="전체 메뉴 열기">`
   - 메뉴 패널: `<div id="mobile-menu" class="header__mobile-menu" aria-hidden="true">`
   - 패널 내 nav + ul 구조 (서브메뉴 포함)

   ## 현재 페이지 표시 (per D-08)
   - `aria-current="page"` 적용 예시
   - 동적으로 현재 페이지 링크에 적용하는 방법 설명

   ## 접근성 JS 예시 (per D-07, D-09)
   - 햄버거 토글 JS (aria-expanded + aria-hidden 동시 업데이트)
   - 메뉴 열기 시 첫 항목 포커스 이동 (D-09)
   - ESC 키 메뉴 닫기 + 트리거 포커스 복귀 (D-09)
   - GNB 서브메뉴 토글 JS (aria-expanded + hidden 속성 토글)
   - ESC 키 서브메뉴 닫기 + 토글 버튼 포커스 복귀
   - 방향키 탐색은 PC GNB 위주 (좌우: 메뉴 간 이동, 상하: 서브메뉴 내 이동) — 예시 수준 제공

   ## 접근성 체크리스트
   - nav에 aria-label 필수
   - 서브메뉴 토글에 aria-expanded 필수
   - 모바일 패널에 aria-hidden 필수
   - 현재 페이지에 aria-current="page"
   - 키보드: Tab으로 모든 메뉴 항목 접근 가능
   - ESC로 열린 메뉴/서브메뉴 닫기 가능

   ## SCSS 파일 참조
   - `scss/6-components/_header.scss` — 헤더 스타일
   - respond-to(pc-sm) 기준으로 PC/모바일 분기
   ```

3. HTML 스니펫에서 사용하는 CSS 클래스명은 06-01-PLAN의 _header.scss와 일치시킨다:
   `header`, `header__inner`, `header__logo`, `header__gnb`, `header__hamburger`, `header__mobile-menu`, `gnb`, `gnb__item`, `gnb__toggle`, `gnb__sub`, `gnb__link`

4. JS 코드 블록은 06-RESEARCH.md의 "GNB JS 토글 스니펫" 섹션을 기반으로 작성하되, 한국어 주석을 충분히 포함한다.

5. role="menu" 사용 금지 경고를 명확히 포함:
   > **주의:** GNB에 `role="menu"`, `role="menuitem"`을 사용하지 않는다.
   > 이는 애플리케이션 컨텍스트 메뉴(우클릭 메뉴)용이며, 사이트 내비게이션에 사용하면
   > 스크린리더가 방향키 전용 탐색을 강제하여 Tab 탐색이 불가해진다. (W3C APG 명시 경고)
  </action>
  <verify>
    <automated>test -f docs/components/header.md && grep -c "aria-expanded" docs/components/header.md && grep -c "aria-hidden" docs/components/header.md && grep -c "aria-current" docs/components/header.md && grep -c "role=\"menu\"" docs/components/header.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/components/header.md` 성공 (파일 존재)
    - `grep "aria-expanded" docs/components/header.md` 출력 있음 (PC + 모바일 양쪽)
    - `grep "aria-hidden" docs/components/header.md` 출력 있음 (모바일 패널)
    - `grep "aria-current" docs/components/header.md` 출력 있음 (현재 페이지 표시)
    - `grep "aria-controls" docs/components/header.md` 출력 있음
    - `grep "header__hamburger" docs/components/header.md` 출력 있음
    - `grep "header__mobile-menu" docs/components/header.md` 출력 있음
    - `grep "gnb__toggle" docs/components/header.md` 출력 있음
    - `grep "gnb__sub" docs/components/header.md` 출력 있음
    - `grep "Escape" docs/components/header.md` 출력 있음 (ESC 키 처리 JS)
    - `grep "focus" docs/components/header.md` 출력 있음 (포커스 관리 JS)
    - `grep "role=\"menu\"" docs/components/header.md` 출력 있음 (금지 경고 문맥)
    - `wc -l < docs/components/header.md` 결과 100 이상
  </acceptance_criteria>
  <done>docs/components/header.md가 PC GNB Disclosure Navigation 패턴 + 모바일 aria-hidden 토글 패턴 + JS 예시 + aria-current 패턴을 모두 포함하고, role="menu" 금지 경고가 명시되어 있다</done>
</task>

</tasks>

<verification>
- `docs/components/header.md` 파일 존재
- PC GNB 스니펫: aria-expanded, aria-controls, gnb__toggle, gnb__sub 포함
- 모바일 스니펫: header__hamburger, header__mobile-menu, aria-hidden 포함
- JS 예시: 햄버거 토글, ESC 닫기, 서브메뉴 토글, 포커스 관리 포함
- aria-current="page" 패턴 포함
- role="menu" 사용 금지 경고 포함
</verification>

<success_criteria>
1. docs/components/header.md 파일이 100줄 이상으로 존재한다
2. PC GNB가 WAI-ARIA Disclosure Navigation 패턴으로 작성되어 있다
3. 모바일 전체메뉴가 aria-hidden 토글 방식으로 작성되어 있다
4. 접근성 JS 예시 코드가 포함되어 있다 (햄버거 토글, ESC, 서브메뉴, 포커스)
5. aria-current="page" 현재 페이지 표시 패턴이 포함되어 있다
6. CSS 클래스명이 _header.scss와 일치한다
</success_criteria>

<output>
After completion, create `.planning/phases/06-핵심-컴포넌트-레이아웃/06-02-SUMMARY.md`
</output>
