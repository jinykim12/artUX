---
phase: 07-핵심-컴포넌트-폼
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scss/6-components/_form.scss
  - scss/6-components/_index.scss
autonomous: true
requirements:
  - COMP-03
must_haves:
  truths:
    - "Bootstrap 폼 클래스(.form-control, .form-select, .form-check-input)가 팀 CSS 토큰으로 재정의된다"
    - "오류/성공 상태(.is-invalid, .is-valid)에 --color-error, --color-success 토큰이 적용된다"
    - "커스텀 체크박스/라디오가 팀 디자인 토큰으로 스타일링된다"
    - "Bootstrap 기본 focus box-shadow가 제거되고 전역 :focus-visible만 동작한다"
  artifacts:
    - path: "scss/6-components/_form.scss"
      provides: "Bootstrap 폼 오버라이드 SCSS"
      contains: ".form-control"
    - path: "scss/6-components/_index.scss"
      provides: "@forward 'form' 추가"
      contains: "@forward 'form'"
  key_links:
    - from: "scss/6-components/_form.scss"
      to: "scss/3-generic/_root.scss"
      via: "CSS 토큰 var(--color-*, --spacing-*, --font-size-*)"
      pattern: "var\\(--"
    - from: "scss/6-components/_index.scss"
      to: "scss/6-components/_form.scss"
      via: "@forward 'form'"
      pattern: "@forward 'form'"
---

<objective>
Bootstrap 5 폼 클래스를 팀 CSS 토큰으로 재정의하는 `_form.scss`를 생성하고, `_index.scss`에 등록한다.

Purpose: 모든 프로젝트에서 일관된 폼 스타일을 즉시 적용할 수 있는 SCSS 기반을 확립한다.
Output: `scss/6-components/_form.scss` (신규), `scss/6-components/_index.scss` (수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-핵심-컴포넌트-폼/07-CONTEXT.md
@.planning/phases/07-핵심-컴포넌트-폼/07-RESEARCH.md

<interfaces>
<!-- 기존 _index.scss 현재 상태 — @forward 'form' 추가 위치 확인 -->
From scss/6-components/_index.scss:
```scss
// ====================================================
// 6-components: UI 컴포넌트 (btn, form, card 등)
// ====================================================
@forward 'skip-nav';  // 본문건너뛰기 (A11Y-01)
@forward 'header';    // 헤더/GNB (COMP-01)
// Phase 6~9에서 추가 컴포넌트 채워짐
```

From scss/3-generic/_root.scss (사용할 토큰):
```scss
:root {
  --color-primary:    #0d6efd;
  --color-secondary:  #6c757d;
  --color-text:       #212529;
  --color-text-muted: #6c757d;
  --color-bg:         #fff;
  --color-border:     #dee2e6;
  --color-error:      #dc3545;
  --color-success:    #198754;
  --font-size-base:   1.6rem;
  --font-size-sm:     1.2rem;
  --font-weight-medium: 500;
  --spacing-xs:       0.4rem;
  --spacing-sm:       0.8rem;
  --spacing-md:       1.6rem;
}
```

From scss/6-components/_header.scss (패턴 참고):
```scss
@use '../2-tools/breakpoints' as *;

// ====================================================
// [헤더 / GNB — COMP-01]
// ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _form.scss 생성 및 _index.scss 등록</name>
  <files>scss/6-components/_form.scss, scss/6-components/_index.scss</files>

  <read_first>
    - scss/6-components/_header.scss (패턴 참고: @use, 한국어 주석, CSS 토큰)
    - scss/6-components/_index.scss (현재 @forward 목록 확인)
    - scss/3-generic/_root.scss (사용할 CSS 토큰 확인)
    - scss/4-elements/_focus.scss (전역 :focus-visible 처리 확인)
    - .planning/phases/07-핵심-컴포넌트-폼/07-RESEARCH.md §Pattern 1 (_form.scss 구조)
  </read_first>

  <action>
    1. `scss/6-components/_form.scss` 신규 생성 (per D-04, D-05, D-06, D-07):

    파일 헤더: `@use '../2-tools/breakpoints' as *;` + COMP-03 주석 블록 (_header.scss 패턴 동일)

    섹션별 구현 (07-RESEARCH.md Pattern 1 기반):

    a) `.form-label` — font-size: var(--font-size-base), font-weight: var(--font-weight-medium), color: var(--color-text), margin-bottom: var(--spacing-xs)

    b) `.form-control` (per D-05) — height: 4.8rem (48px 터치 타겟), padding: var(--spacing-sm) var(--spacing-md), font-size: var(--font-size-base), color: var(--color-text), background-color: var(--color-bg), border: 1px solid var(--color-border), border-radius: 0.4rem, transition: none (공공기관 원칙)
       - `&:focus` — border-color: var(--color-primary), box-shadow: none, outline: 0 (전역 :focus-visible이 처리)
       - `&[rows]` — height: auto (textarea용)
       - `&.is-invalid` — border-color: var(--color-error) (per D-06)
       - `&.is-valid` — border-color: var(--color-success) (per D-06)

    c) `.form-select` — .form-control과 동일한 높이/패딩/포커스/상태 스타일

    d) `.form-check-input` (per D-07) — width: 2.0rem, height: 2.0rem, border: 2px solid var(--color-border), cursor: pointer
       - `&:checked` — background-color: var(--color-primary), border-color: var(--color-primary)
       - `&:focus` — box-shadow: none

    e) `.invalid-feedback, .form-error` — color: var(--color-error), font-size: var(--font-size-sm), margin-top: var(--spacing-xs)

    f) `.valid-feedback, .form-success` — color: var(--color-success), font-size: var(--font-size-sm), margin-top: var(--spacing-xs)

    g) `.form-text, .form-hint` — color: var(--color-text-muted), font-size: var(--font-size-sm), margin-top: var(--spacing-xs)

    h) `fieldset legend.form-label` — margin-bottom: var(--spacing-xs) (Pitfall 4: Bootstrap _reboot.scss legend margin-bottom 이중 방지)

    주의사항:
    - Sass 변수 오버라이드 방식 금지 ($input-border-color 등 사용 금지) — CSS 선택자 오버라이드만 사용
    - 트랜지션 애니메이션 추가 금지 (공공기관 즉시 표시 원칙)
    - Bootstrap SVG 체크마크 커스텀 재작성 금지 — Bootstrap --bs-form-check-bg-image 유지 (고대비 모드 대응)

    2. `scss/6-components/_index.scss` 수정 (per D-08):
    `@forward 'header';` 아래에 `@forward 'form';    // 폼 컴포넌트 (COMP-03)` 추가
  </action>

  <verify>
    <automated>npm run build:css 2>&1 && grep -c "form-control" dist/artux.css && grep "@forward 'form'" scss/6-components/_index.scss</automated>
  </verify>

  <acceptance_criteria>
    - grep "form-control" scss/6-components/_form.scss returns matches
    - grep "form-select" scss/6-components/_form.scss returns matches
    - grep "form-check-input" scss/6-components/_form.scss returns matches
    - grep "is-invalid" scss/6-components/_form.scss returns matches
    - grep "is-valid" scss/6-components/_form.scss returns matches
    - grep "var(--color-error)" scss/6-components/_form.scss returns matches
    - grep "var(--color-success)" scss/6-components/_form.scss returns matches
    - grep "box-shadow: none" scss/6-components/_form.scss returns matches
    - grep "transition: none" scss/6-components/_form.scss returns matches
    - grep "@forward 'form'" scss/6-components/_index.scss returns match
    - npm run build:css exits 0 (SCSS 컴파일 성공)
    - grep "form-control" dist/artux.css returns matches (CSS 출력에 포함)
  </acceptance_criteria>

  <done>
    _form.scss가 .form-control, .form-select, .form-check-input, .form-label, 오류/성공 상태를 CSS 토큰으로 재정의하고, _index.scss에 @forward 'form'이 등록되어, npm run build:css로 dist/artux.css에 폼 스타일이 출력된다.
  </done>
</task>

</tasks>

<verification>
1. `npm run build:css` — SCSS 컴파일 에러 없음
2. `grep -c "form-control" dist/artux.css` — 0보다 큼
3. `grep "@forward 'form'" scss/6-components/_index.scss` — 매치 확인
4. `grep "box-shadow: none" scss/6-components/_form.scss` — Bootstrap focus ring 제거 확인
5. `grep "transition: none" scss/6-components/_form.scss` — 트랜지션 없음 확인
</verification>

<success_criteria>
- _form.scss가 7개 섹션(label, control, select, check, invalid, valid, text)을 모두 포함
- 모든 스타일 값이 CSS 토큰(var(--*))을 사용 (Sass 변수 미사용)
- _index.scss에 @forward 'form' 등록 완료
- npm run build:css 정상 컴파일
</success_criteria>

<output>
After completion, create `.planning/phases/07-핵심-컴포넌트-폼/07-01-SUMMARY.md`
</output>
