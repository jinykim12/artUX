---
phase: 08-핵심-컴포넌트-UI
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scss/6-components/_table.scss
  - scss/6-components/_breadcrumb.scss
  - docs/components/table.md
  - docs/components/pagination.md
  - docs/components/breadcrumb.md
autonomous: true
requirements: [COMP-05, COMP-08, COMP-09]

must_haves:
  truths:
    - "테이블 컴포넌트에 caption, scope 속성이 포함된 마크업 패턴이 존재한다"
    - "테이블에 반응형 스크롤 wrapper 패턴이 제공된다"
    - "페이지네이션에 aria-label='페이지 탐색'과 aria-current='page'가 적용된다"
    - "브레드크럼에 nav aria-label='breadcrumb'이 사용된다"
    - "table SCSS에 Bootstrap .table 오버라이드가 CSS 토큰으로 정의된다"
    - "breadcrumb SCSS에 구분자 커스텀과 색상 토큰이 적용된다"
  artifacts:
    - path: "scss/6-components/_table.scss"
      provides: "Bootstrap .table 오버라이드 + 반응형 wrapper"
      contains: ".table"
    - path: "scss/6-components/_breadcrumb.scss"
      provides: "Bootstrap .breadcrumb 오버라이드 + 구분자 커스텀"
      contains: ".breadcrumb"
    - path: "docs/components/table.md"
      provides: "테이블 마크업 패턴 문서"
      min_lines: 40
    - path: "docs/components/pagination.md"
      provides: "페이지네이션 마크업 문서 (SCSS 없음)"
      min_lines: 30
    - path: "docs/components/breadcrumb.md"
      provides: "브레드크럼 마크업 패턴 문서"
      min_lines: 20
  key_links:
    - from: "docs/components/table.md"
      to: "scss/6-components/_table.scss"
      via: "클래스명 참조"
      pattern: "table-responsive"
    - from: "docs/components/breadcrumb.md"
      to: "scss/6-components/_breadcrumb.scss"
      via: "클래스명 참조"
      pattern: "breadcrumb"
---

<objective>
Table, Pagination, Breadcrumb 컴포넌트 SCSS 및 마크업 문서를 작성한다.

Purpose: COMP-05(테이블), COMP-08(페이지네이션), COMP-09(브레드크럼) 요구사항을 충족한다. Pagination은 D-10에 따라 Bootstrap 기본 스타일 유지(별도 SCSS 불필요), 마크업 문서만 작성한다.
Output: _table.scss, _breadcrumb.scss, table.md, pagination.md, breadcrumb.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-핵심-컴포넌트-UI/08-CONTEXT.md

@scss/6-components/_form.scss
@scss/6-components/_header.scss
@scss/3-generic/_root.scss
@docs/components/forms.md
@docs/components/header.md

<interfaces>
<!-- 기존 CSS 토큰 (scss/3-generic/_root.scss) -->
--color-primary, --color-secondary, --color-text, --color-text-muted
--color-bg, --color-border, --color-error, --color-success
--font-size-sm, --font-size-base, --font-size-md
--font-weight-regular, --font-weight-medium, --font-weight-bold
--spacing-xs, --spacing-sm, --spacing-md, --spacing-lg
--shadow-sm, --shadow-md, --shadow-lg

<!-- _index.scss @forward는 Plan 01에서 이미 추가됨 — 이 Plan에서 _index.scss 수정 금지 -->
<!-- Plan 01이 @forward 'table'; @forward 'breadcrumb'; 를 미리 추가해둠 -->

<!-- Phase 7 패턴: CSS 선택자 오버라이드, box-shadow: none, transition: none -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table + Breadcrumb SCSS 작성</name>
  <files>scss/6-components/_table.scss, scss/6-components/_breadcrumb.scss</files>
  <read_first>scss/6-components/_form.scss, scss/6-components/_header.scss, scss/3-generic/_root.scss</read_first>
  <action>
**_table.scss 작성 (per D-08, COMP-05):**

Phase 7 _form.scss 패턴과 동일한 구조. 파일 상단에 한국어 주석 블록(컴포넌트명, 작성일, 사용 예 HTML, 접근성 주의사항) 포함.

```
@use '../2-tools/breakpoints' as *;
```

1. `.table`:
   - width: 100%
   - color: var(--color-text)
   - font-size: var(--font-size-base)
   - border-color: var(--color-border)

2. `.table thead th`:
   - font-weight: var(--font-weight-bold)
   - color: var(--color-text)
   - background-color: var(--color-border) (살짝 회색 배경으로 헤더 구분)
   - padding: var(--spacing-sm) var(--spacing-md)
   - border-bottom: 2px solid var(--color-border)
   - white-space: nowrap

3. `.table tbody td`:
   - padding: var(--spacing-sm) var(--spacing-md)
   - border-bottom: 1px solid var(--color-border)
   - vertical-align: middle

4. `.table caption`:
   - caption-side: top
   - padding: var(--spacing-sm) 0
   - font-size: var(--font-size-sm)
   - color: var(--color-text-muted)
   - text-align: left

5. `.table-responsive` (반응형 스크롤 wrapper):
   - overflow-x: auto
   - -webkit-overflow-scrolling: touch
   - 중요: Bootstrap .table-responsive와 동일 네이밍 유지 (Bootstrap 오버라이드)

**_breadcrumb.scss 작성 (per D-09, COMP-09):**

파일 상단에 한국어 주석 블록 포함.

1. `.breadcrumb`:
   - display: flex
   - flex-wrap: wrap
   - padding: 0
   - margin-bottom: var(--spacing-md)
   - list-style: none
   - font-size: var(--font-size-sm)

2. `.breadcrumb-item`:
   - color: var(--color-text-muted)
   - a 태그: color: var(--color-primary); text-decoration: none; &:hover { text-decoration: underline; }

3. `.breadcrumb-item + .breadcrumb-item` (구분자 커스텀):
   - &::before 의 content를 "/" 또는 ">" 로 설정
   - padding: 0 var(--spacing-xs)
   - color: var(--color-text-muted)

4. `.breadcrumb-item.active` (현재 페이지):
   - color: var(--color-text)
   - font-weight: var(--font-weight-medium)
  </action>
  <verify>
    <automated>grep -q ".table" scss/6-components/_table.scss && grep -q ".breadcrumb" scss/6-components/_breadcrumb.scss && grep -q "table-responsive" scss/6-components/_table.scss && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- scss/6-components/_table.scss에 .table, .table thead th, .table tbody td, .table caption, .table-responsive가 정의되어 있다
- scss/6-components/_breadcrumb.scss에 .breadcrumb, .breadcrumb-item, 구분자 ::before가 정의되어 있다
- 모든 SCSS에서 CSS 토큰(var(--*))만 사용하고 Sass 변수 오버라이드 없다
- 한국어 주석 블록이 파일 상단에 있다
- _index.scss는 수정하지 않았다 (Plan 01에서 처리)
  </acceptance_criteria>
  <done>Table, Breadcrumb SCSS 파일이 팀 패턴대로 작성되었다</done>
</task>

<task type="auto">
  <name>Task 2: table.md + pagination.md + breadcrumb.md 마크업 문서 작성</name>
  <files>docs/components/table.md, docs/components/pagination.md, docs/components/breadcrumb.md</files>
  <read_first>docs/components/forms.md, docs/components/header.md, scss/6-components/_table.scss, scss/6-components/_breadcrumb.scss</read_first>
  <action>
**table.md 작성 (per D-01, D-02, D-03, COMP-05):**

Phase 7 forms.md 패턴 따름. 상단 메타(KWCAG 기준, 적용 수준, 작성일, SCSS 참조).

포함 섹션:

1. **기본 테이블**
```html
<!-- 기본 데이터 테이블 — caption 필수, th에 scope 필수 -->
<table class="table">
  <caption>사용자 목록 (총 3명)</caption>
  <thead>
    <tr>
      <th scope="col">이름</th>
      <th scope="col">이메일</th>
      <th scope="col">가입일</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>홍길동</td>
      <td>hong@example.com</td>
      <td>2026-01-15</td>
    </tr>
  </tbody>
</table>
```

2. **행 헤더 테이블** (scope="row" 예시)
```html
<table class="table">
  <caption>분기별 매출 현황</caption>
  <thead>
    <tr>
      <th scope="col">분기</th>
      <th scope="col">매출</th>
      <th scope="col">전년 대비</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1분기</th>
      <td>1,200만원</td>
      <td>+15%</td>
    </tr>
  </tbody>
</table>
```

3. **반응형 테이블** (스크롤 wrapper)
```html
<!-- 반응형 — 모바일에서 수평 스크롤 -->
<div class="table-responsive">
  <table class="table">
    <caption>상세 데이터 테이블</caption>
    <!-- thead/tbody 동일 -->
  </table>
</div>
```

4. **접근성 체크리스트**:
   - caption 필수 — 테이블 용도 설명 (스크린 리더 사전 안내)
   - 열 헤더 th에 scope="col" 필수
   - 행 헤더 th에 scope="row" 필수
   - 복잡한 테이블은 id/headers 속성 사용
   - 레이아웃 용도로 table 사용 금지

**pagination.md 작성 (per D-01, D-02, D-03, D-10, COMP-08):**

별도 SCSS 없음(Bootstrap 기본 유지 per D-10). 마크업 문서만 작성.

포함 섹션:

1. **기본 페이지네이션**
```html
<!-- 페이지네이션 — nav + aria-label 필수 -->
<nav aria-label="페이지 탐색">
  <ul class="pagination">
    <li class="page-item">
      <a class="page-link" href="#" aria-label="이전 페이지">
        <span aria-hidden="true">&laquo;</span>
      </a>
    </li>
    <li class="page-item"><a class="page-link" href="#">1</a></li>
    <li class="page-item active" aria-current="page">
      <span class="page-link">2</span>
    </li>
    <li class="page-item"><a class="page-link" href="#">3</a></li>
    <li class="page-item">
      <a class="page-link" href="#" aria-label="다음 페이지">
        <span aria-hidden="true">&raquo;</span>
      </a>
    </li>
  </ul>
</nav>
```

2. **비활성 이전/다음**
```html
<li class="page-item disabled">
  <span class="page-link" aria-label="이전 페이지 (없음)">
    <span aria-hidden="true">&laquo;</span>
  </span>
</li>
```

3. **접근성 체크리스트**:
   - nav 태그에 aria-label="페이지 탐색" 필수
   - 현재 페이지에 aria-current="page" 필수
   - 현재 페이지는 a 대신 span 사용 (클릭 불가 표시)
   - 이전/다음 버튼에 aria-label 필수 (시각적 기호만으로 불충분)
   - disabled 상태에서 a 대신 span 사용

**breadcrumb.md 작성 (per D-01, D-02, D-03, COMP-09):**

포함 섹션:

1. **기본 브레드크럼**
```html
<!-- 브레드크럼 — nav + aria-label 필수, 마지막 항목 aria-current="page" -->
<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/">홈</a></li>
    <li class="breadcrumb-item"><a href="/board">게시판</a></li>
    <li class="breadcrumb-item active" aria-current="page">공지사항</li>
  </ol>
</nav>
```

2. **접근성 체크리스트**:
   - nav 태그에 aria-label="breadcrumb" 필수
   - ol 순서 목록 사용 (페이지 계층 순서 의미)
   - 마지막 항목(현재 페이지)에 aria-current="page" 필수
   - 현재 페이지는 링크 없이 텍스트만 (클릭 불가)
  </action>
  <verify>
    <automated>test -f docs/components/table.md && test -f docs/components/pagination.md && test -f docs/components/breadcrumb.md && grep -q 'scope="col"' docs/components/table.md && grep -q 'aria-current="page"' docs/components/pagination.md && grep -q 'aria-label="breadcrumb"' docs/components/breadcrumb.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- docs/components/table.md가 존재하고 caption, scope="col", scope="row", table-responsive 마크업이 포함되어 있다
- docs/components/pagination.md가 존재하고 aria-label="페이지 탐색", aria-current="page" 마크업이 포함되어 있다
- docs/components/breadcrumb.md가 존재하고 nav aria-label="breadcrumb", aria-current="page" 마크업이 포함되어 있다
- 세 문서 모두 접근성 체크리스트 섹션이 있다
- 세 문서 모두 forms.md와 동일한 마크다운 구조를 따른다
  </acceptance_criteria>
  <done>table.md, pagination.md, breadcrumb.md가 팀 문서 패턴대로 작성되어 COMP-05, COMP-08, COMP-09의 마크업 문서 요구사항을 충족한다</done>
</task>

</tasks>

<verification>
1. `scss/6-components/_table.scss`에 .table, .table-responsive, caption 스타일이 있다
2. `scss/6-components/_breadcrumb.scss`에 .breadcrumb, 구분자 ::before가 있다
3. `docs/components/table.md`에 caption, scope 접근성 패턴이 있다
4. `docs/components/pagination.md`에 aria-label="페이지 탐색", aria-current="page"가 있다
5. `docs/components/breadcrumb.md`에 nav aria-label="breadcrumb"가 있다
6. _index.scss를 수정하지 않았다 (Plan 01에서 처리 완료)
</verification>

<success_criteria>
- Table SCSS에 반응형 wrapper(.table-responsive)가 포함된다
- Breadcrumb SCSS에 구분자 커스텀과 색상 토큰이 적용된다
- Pagination은 SCSS 없이 마크업 문서만 존재한다 (per D-10)
- 세 문서 모두 KWCAG 2.1 AA 접근성 패턴(caption, scope, aria-label, aria-current)이 포함된다
</success_criteria>

<output>
After completion, create `.planning/phases/08-핵심-컴포넌트-UI/08-02-SUMMARY.md`
</output>
