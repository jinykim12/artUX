---
phase: 09-핵심-컴포넌트-오버레이
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scss/6-components/_index.scss
  - scss/6-components/_modal.scss
  - scss/6-components/_tab.scss
  - docs/components/modal.md
  - docs/components/tab.md
autonomous: true
requirements: [COMP-06, COMP-07, A11Y-09]
must_haves:
  truths:
    - "모달 SCSS에 Bootstrap .modal 오버라이드 + CSS 토큰 + z-index 토큰이 적용되어 있다"
    - "탭 SCSS에 Bootstrap .nav-tabs 오버라이드 + aria-selected 상태 스타일이 있다"
    - "모달 문서에 role=dialog, aria-modal, 포커스 트랩 JS 예시가 포함되어 있다"
    - "탭 문서에 role=tablist/tab/tabpanel + 방향키 탐색 JS 예시가 포함되어 있다"
    - "_index.scss에 modal, tab, slider @forward 3종이 추가되어 있다"
  artifacts:
    - path: "scss/6-components/_index.scss"
      provides: "@forward 3종 (modal, tab, slider)"
      contains: "@forward 'modal'"
    - path: "scss/6-components/_modal.scss"
      provides: "모달 오버레이/배경/콘텐츠 스타일"
      contains: ".modal"
    - path: "scss/6-components/_tab.scss"
      provides: "탭 선택 상태 스타일"
      contains: ".nav-tabs"
    - path: "docs/components/modal.md"
      provides: "모달 마크업 패턴 + 포커스 트랩 JS"
      contains: "role=\"dialog\""
    - path: "docs/components/tab.md"
      provides: "탭 마크업 패턴 + 방향키 JS"
      contains: "role=\"tablist\""
  key_links:
    - from: "scss/6-components/_index.scss"
      to: "scss/6-components/_modal.scss"
      via: "@forward 'modal'"
      pattern: "@forward 'modal'"
    - from: "scss/6-components/_index.scss"
      to: "scss/6-components/_tab.scss"
      via: "@forward 'tab'"
      pattern: "@forward 'tab'"
    - from: "docs/components/modal.md"
      to: "docs/accessibility/dynamic-content.md"
      via: "상호 참조 링크"
      pattern: "dynamic-content"
---

<objective>
모달(COMP-06) + 탭(COMP-07) SCSS/문서 작성 및 _index.scss @forward 3종 일괄 추가

Purpose: Bootstrap 모달/탭 오버라이드 SCSS와 KWCAG 2.1 AA 접근성 마크업 문서를 제공한다. _index.scss에 Phase 9 전체 3종(modal, tab, slider) @forward를 이 플랜에서 일괄 추가하여 race condition을 방지한다.
Output: _modal.scss, _tab.scss, _index.scss 수정, modal.md, tab.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-핵심-컴포넌트-오버레이/09-CONTEXT.md

@scss/6-components/_index.scss
@scss/6-components/_button.scss
@scss/6-components/_table.scss
@scss/3-generic/_root.scss
@docs/components/header.md
@docs/components/button.md
@docs/accessibility/dynamic-content.md

<interfaces>
From scss/3-generic/_root.scss (사용할 토큰):
```scss
--z-overlay:  1040;
--z-modal:    1050;
--transition-base: 300ms ease;
--shadow-md: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
--color-bg: #fff;
--color-text: #212529;
--color-border: #dee2e6;
--spacing-md: 1.6rem;
--spacing-lg: 2.4rem;
```

From scss/6-components/_index.scss (현재 상태):
```scss
@forward 'skip-nav';
@forward 'header';
@forward 'form';
@forward 'button';
@forward 'card';
@forward 'table';
@forward 'breadcrumb';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _index.scss @forward 3종 + _modal.scss + modal.md</name>
  <files>scss/6-components/_index.scss, scss/6-components/_modal.scss, docs/components/modal.md</files>
  <read_first>
    - scss/6-components/_index.scss (현재 @forward 목록 확인)
    - scss/6-components/_button.scss (SCSS 패턴 참고 — 헤더 주석, @use, 선택자 구조)
    - scss/3-generic/_root.scss (z-index, overlay 토큰 확인)
    - docs/components/header.md (문서 구조 패턴 — KWCAG 기준, 코드 블록 + 접근성 주석)
    - docs/accessibility/dynamic-content.md (포커스 트랩 원칙 참조)
  </read_first>
  <action>
**1) scss/6-components/_index.scss 수정 (per D-04)**
기존 7종 뒤에 Phase 9 @forward 3종 일괄 추가:
```scss
@forward 'modal';       // 모달 (COMP-06)
@forward 'tab';         // 탭 (COMP-07)
@forward 'slider';      // 슬라이더 (COMP-10) — Plan 02에서 생성
```

**2) scss/6-components/_modal.scss 생성 (per D-03, D-05)**
Phase 8 _button.scss 패턴 동일 적용:
- `@use '../2-tools/breakpoints' as *;` 상단 임포트
- 헤더 주석 블록: [모달 컴포넌트 — COMP-06], 사용 예 HTML, 접근성 주의사항
- Bootstrap `.modal` 오버라이드 — CSS 토큰만 사용 (Sass 변수 금지)
- `.modal` — transition: none (공공기관 즉시 표시 원칙)
- `.modal-backdrop` — background-color: rgba(0, 0, 0, 0.5), z-index: var(--z-overlay)
- `.modal-dialog` — z-index: var(--z-modal)
- `.modal-content` — border-radius: 0.4rem, box-shadow: var(--shadow-md), background: var(--color-bg)
- `.modal-header` — padding: var(--spacing-lg), border-bottom: 1px solid var(--color-border)
- `.modal-body` — padding: var(--spacing-lg)
- `.modal-footer` — padding: var(--spacing-md) var(--spacing-lg), border-top: 1px solid var(--color-border)
- `.modal .btn-close` 포커스 스타일은 전역 _focus.scss에서 처리 (중복 금지)

**3) docs/components/modal.md 생성 (per D-01, D-02, D-06, D-09)**
Phase 8 header.md/button.md 문서 구조 패턴 적용:
- 제목: `# 모달 (COMP-06)`
- KWCAG 2.1 기준 명시: 2.1.1 키보드 접근, 2.4.3 초점 순서, 4.1.2 이름/역할/값
- 기본 구조 설명 (role="dialog", aria-modal="true", aria-labelledby)
- 완전한 HTML 마크업 코드 블록 — role="dialog", aria-modal="true", aria-labelledby="modal-title", 닫기 버튼 aria-label="닫기"
- 접근성 체크리스트 (aria-modal, aria-labelledby, 포커스 트랩, ESC 닫기)
- **포커스 트랩 JS 예시 코드** (per D-06): focusableElements 선택자, Tab/Shift+Tab 순환 로직, 모달 열림 시 첫 포커스 이동, 닫힘 시 트리거 요소 복원
- `docs/accessibility/dynamic-content.md` 상호 참조 링크 (per D-09)
  </action>
  <verify>
    <automated>grep -c "@forward 'modal'" scss/6-components/_index.scss && grep -c "@forward 'tab'" scss/6-components/_index.scss && grep -c "@forward 'slider'" scss/6-components/_index.scss && grep -c "role=\"dialog\"" docs/components/modal.md && grep -c "aria-modal" docs/components/modal.md && grep -c "focusable" docs/components/modal.md && grep -c "var(--z-modal)" scss/6-components/_modal.scss</automated>
  </verify>
  <acceptance_criteria>
    - grep "@forward 'modal'" scss/6-components/_index.scss => 1 match
    - grep "@forward 'tab'" scss/6-components/_index.scss => 1 match
    - grep "@forward 'slider'" scss/6-components/_index.scss => 1 match
    - grep "z-modal" scss/6-components/_modal.scss => 1+ match
    - grep "z-overlay" scss/6-components/_modal.scss => 1+ match
    - grep "transition: none" scss/6-components/_modal.scss => 1+ match (공공기관 원칙)
    - grep "var(--" scss/6-components/_modal.scss => 5+ matches (CSS 토큰 사용)
    - grep 'role="dialog"' docs/components/modal.md => 1+ match
    - grep "aria-modal" docs/components/modal.md => 1+ match
    - grep "aria-labelledby" docs/components/modal.md => 1+ match
    - grep "focusable" docs/components/modal.md => 1+ match (포커스 트랩 JS)
    - grep "dynamic-content" docs/components/modal.md => 1+ match (상호 참조)
  </acceptance_criteria>
  <done>
    - _index.scss에 modal, tab, slider @forward 3종이 기존 7종 뒤에 추가됨
    - _modal.scss가 Bootstrap .modal 오버라이드로 생성됨 (CSS 토큰, z-index 토큰, transition: none)
    - modal.md에 role="dialog" + aria-modal + 포커스 트랩 JS 예시 + dynamic-content.md 참조 포함
  </done>
</task>

<task type="auto">
  <name>Task 2: _tab.scss + tab.md</name>
  <files>scss/6-components/_tab.scss, docs/components/tab.md</files>
  <read_first>
    - scss/6-components/_button.scss (SCSS 패턴 참고)
    - scss/3-generic/_root.scss (토큰 확인)
    - docs/components/header.md (문서 구조 패턴)
    - docs/accessibility/dynamic-content.md (aria-live 참조)
  </read_first>
  <action>
**1) scss/6-components/_tab.scss 생성 (per D-03, D-05)**
Phase 8 _button.scss 패턴 동일 적용:
- `@use '../2-tools/breakpoints' as *;` 상단 임포트
- 헤더 주석 블록: [탭 컴포넌트 — COMP-07], 사용 예 HTML, 접근성 주의사항
- Bootstrap `.nav-tabs` 오버라이드 — CSS 토큰만 사용
- `.nav-tabs .nav-link` — padding: var(--spacing-sm) var(--spacing-md), color: var(--color-text-muted), border: none, transition: none (공공기관)
- `.nav-tabs .nav-link.active`, `.nav-tabs .nav-link[aria-selected="true"]` — color: var(--color-primary), border-bottom: 2px solid var(--color-primary), font-weight: var(--font-weight-medium)
- `.nav-tabs .nav-link:hover` — color: var(--color-text)
- `.tab-content > .tab-pane` — padding: var(--spacing-lg) 0
- 포커스 스타일은 전역 _focus.scss에서 처리 (중복 금지)

**2) docs/components/tab.md 생성 (per D-01, D-02, D-07, D-09)**
- 제목: `# 탭 (COMP-07)`
- KWCAG 2.1 기준: 2.1.1 키보드 접근, 4.1.2 이름/역할/값
- 완전한 HTML 마크업 — role="tablist" 컨테이너, role="tab" 각 탭 버튼 (aria-selected, aria-controls), role="tabpanel" 콘텐츠 영역 (aria-labelledby)
- 접근성 체크리스트 (tablist, tab, tabpanel, aria-selected, aria-controls, aria-labelledby)
- **방향키 탐색 JS 예시 코드** (per D-07): ArrowLeft/ArrowRight로 탭 이동, Home/End 키, 활성 탭 focus + aria-selected 토글
- `docs/accessibility/dynamic-content.md` 상호 참조 링크 (per D-09)
  </action>
  <verify>
    <automated>grep -c "aria-selected" scss/6-components/_tab.scss && grep -c "role=\"tablist\"" docs/components/tab.md && grep -c "role=\"tab\"" docs/components/tab.md && grep -c "ArrowRight\|ArrowLeft" docs/components/tab.md && grep -c "var(--" scss/6-components/_tab.scss</automated>
  </verify>
  <acceptance_criteria>
    - grep "aria-selected" scss/6-components/_tab.scss => 1+ match (CSS attribute selector)
    - grep "nav-tabs" scss/6-components/_tab.scss => 1+ match
    - grep "transition: none" scss/6-components/_tab.scss => 1+ match
    - grep "var(--" scss/6-components/_tab.scss => 4+ matches (CSS 토큰 사용)
    - grep 'role="tablist"' docs/components/tab.md => 1+ match
    - grep 'role="tab"' docs/components/tab.md => 1+ match
    - grep 'role="tabpanel"' docs/components/tab.md => 1+ match
    - grep "aria-selected" docs/components/tab.md => 1+ match
    - grep "aria-controls" docs/components/tab.md => 1+ match
    - grep "ArrowRight\|ArrowLeft" docs/components/tab.md => 1+ match (방향키 JS)
    - grep "dynamic-content" docs/components/tab.md => 1+ match (상호 참조)
  </acceptance_criteria>
  <done>
    - _tab.scss가 Bootstrap .nav-tabs 오버라이드로 생성됨 (CSS 토큰, aria-selected attribute selector, transition: none)
    - tab.md에 role="tablist/tab/tabpanel" ARIA 패턴 + 방향키 JS 예시 + dynamic-content.md 참조 포함
  </done>
</task>

</tasks>

<verification>
1. `npx sass scss/style.scss dist/artux.css --no-source-map` 컴파일 오류 없음
2. _index.scss에 @forward 10종 (기존 7 + Phase 9 3종) 확인
3. modal.md에 role="dialog" + aria-modal + 포커스 트랩 JS 포함
4. tab.md에 role="tablist" + role="tab" + 방향키 JS 포함
</verification>

<success_criteria>
- _index.scss에 modal, tab, slider @forward 3종이 추가되어 SCSS 컴파일 시 오류 없음
- _modal.scss가 CSS 토큰 + z-index 토큰 + transition: none으로 Bootstrap .modal 오버라이드
- _tab.scss가 CSS 토큰 + aria-selected attribute selector로 Bootstrap .nav-tabs 오버라이드
- modal.md에 KWCAG 2.1 AA 접근성 마크업 + 포커스 트랩 JS 예시 코드 포함
- tab.md에 KWCAG 2.1 AA 접근성 마크업 + 방향키 탐색 JS 예시 코드 포함
</success_criteria>

<output>
After completion, create `.planning/phases/09-핵심-컴포넌트-오버레이/09-01-SUMMARY.md`
</output>
