---
phase: 10-eleventy-문서-사이트
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - eleventy.config.js
  - docs/_includes/layouts/base.njk
  - docs/_data/navigation.js
  - docs/index.md
  - package.json
  - .gitignore
autonomous: true
requirements:
  - DOCS-01
  - DOCS-07

must_haves:
  truths:
    - "npm run serve 실행 시 Eleventy dev 서버가 localhost:8080에서 응답한다"
    - "npm run build 실행 시 _site/ 디렉토리가 생성된다"
    - "브라우저에서 localhost:8080 접속 시 사이드 네비게이션이 렌더된다"
    - "dist/artux.css가 _site/에 복사되어 스타일이 적용된다"
  artifacts:
    - path: "eleventy.config.js"
      provides: "Eleventy 3.x ESM 설정 (input: docs, output: _site)"
    - path: "docs/_includes/layouts/base.njk"
      provides: "공통 베이스 레이아웃 (사이드 네비 + 콘텐츠)"
    - path: "docs/_data/navigation.js"
      provides: "사이드 네비 섹션 데이터"
    - path: "package.json"
      provides: "serve, build npm 스크립트"
  key_links:
    - from: "docs/_includes/layouts/base.njk"
      to: "docs/_data/navigation.js"
      via: "Nunjucks {{ navigation }} 전역 데이터"
    - from: "package.json (serve)"
      to: "eleventy.config.js"
      via: "npm-run-all2 --parallel watch:css + eleventy --serve"
---

<objective>
Eleventy 3.x를 설치하고, 기본 설정 + 베이스 레이아웃 + npm 스크립트를 구성하여 문서 사이트의 실행 환경을 완성한다.

Purpose: 이후 모든 콘텐츠 페이지가 동작할 기반 레이어. DOCS-01 (npm run serve) 과 DOCS-07 (npm run build) 직접 충족.
Output: eleventy.config.js, base.njk 레이아웃, navigation 데이터, 업데이트된 package.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-eleventy-문서-사이트/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eleventy 설치 + eleventy.config.js + .gitignore 업데이트</name>
  <files>eleventy.config.js, package.json, .gitignore</files>
  <read_first>
    - package.json (기존 scripts, devDependencies 확인)
    - .gitignore (기존 항목 확인)
  </read_first>
  <action>
1. @11ty/eleventy 3.x 설치:
   ```
   npm install --save-dev @11ty/eleventy@^3.0.0
   ```

2. eleventy.config.js 생성 (ESM 방식, per D-01):
   ```js
   export default function (eleventyConfig) {
     // dist/artux.css passthrough 복사 (per D-03)
     eleventyConfig.addPassthroughCopy({ "dist/artux.css": "artux.css" });
     // docs/js/ 복사 (Plan 03에서 생성할 copy.js 대비)
     eleventyConfig.addPassthroughCopy({ "docs/js": "js" });

     return {
       dir: {
         input: "docs",      // per D-02
         output: "_site",    // per D-02
         includes: "_includes",
         data: "_data",
       },
       templateFormats: ["njk", "md", "html"],
       markdownTemplateEngine: "njk",  // 마크다운 내 Nunjucks 허용
       htmlTemplateEngine: "njk",
     };
   }
   ```
   파일 최상단에 주석: `// Eleventy 3.x ESM 설정 — per D-01, D-02, D-03`

3. package.json scripts 업데이트 (기존 build:css, watch:css 유지, per D-13, D-14):
   - `"serve": "npm-run-all2 --parallel watch:css eleventy:serve"`
   - `"eleventy:serve": "eleventy --serve"`
   - `"eleventy:build": "eleventy"`
   - `"build": "npm run build:css && npm run eleventy:build"` (per D-14, 순차 실행)

4. .gitignore에 추가 (없는 경우만):
   ```
   _site/
   ```

5. package.json의 `"type": "module"` 필드 추가 (ESM eleventy.config.js 로드를 위해 필수).
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && node -e "import('./eleventy.config.js').then(m => console.log('OK:', typeof m.default))" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - eleventy.config.js 파일 존재, export default function 포함
    - package.json에 "type": "module" 존재
    - package.json scripts에 "serve", "build", "eleventy:serve", "eleventy:build" 4종 존재
    - .gitignore에 _site/ 포함
    - grep "passthrough\|_site\|artux.css" eleventy.config.js → 3개 이상 매치
  </acceptance_criteria>
  <done>eleventy.config.js + package.json 업데이트 완료, node로 ESM import 정상 확인</done>
</task>

<task type="auto">
  <name>Task 2: 베이스 레이아웃(base.njk) + 사이드 네비 데이터(_data/navigation.js) + 홈 인덱스 페이지</name>
  <files>docs/_includes/layouts/base.njk, docs/_data/navigation.js, docs/index.md</files>
  <read_first>
    - .planning/phases/10-eleventy-문서-사이트/10-CONTEXT.md (D-05, D-06 네비 섹션 목록 확인)
    - docs/components/button.md (기존 마크다운 구조 파악)
  </read_first>
  <action>
1. docs/_data/navigation.js 생성 (per D-06, ESM):
   ```js
   export default [
     { title: "시작하기",    url: "/",             section: "getting-started" },
     { title: "SCSS 구조",   url: "/scss/",        section: "scss" },
     { title: "디자인 토큰", url: "/tokens/",      section: "tokens" },
     { title: "반응형",      url: "/responsive/",  section: "responsive" },
     { title: "접근성",      url: "/accessibility/",section: "accessibility" },
     { title: "컴포넌트",    url: "/components/",  section: "components" },
     { title: "스타터 킷",  url: "/starter/",     section: "starter" },
     { title: "컨벤션",      url: "/conventions/", section: "conventions" },
   ];
   ```

2. docs/_includes/layouts/base.njk 생성 (per D-05):
   - HTML5 문서 구조, lang="ko", charset="UTF-8"
   - `<link rel="stylesheet" href="/artux.css">` (passthrough 복사된 artux.css)
   - `<link rel="stylesheet" href="/docs.css">` (Plan 03에서 생성 예정 — 없어도 빌드 오류 없음)
   - 레이아웃: `.docs-layout` (display:flex) — `.docs-sidebar` + `.docs-content`
   - 사이드바: `<nav aria-label="문서 네비게이션">` + navigation 데이터 루프 → `<ul>` 링크 목록
   - 현재 페이지 활성화: Nunjucks `{% if page.url == item.url %}class="is-active"{% endif %}`
   - 모바일 토글 버튼 (per D-07): `<button class="docs-nav-toggle" aria-expanded="false" aria-controls="docs-sidebar">메뉴</button>`
   - `{{ content | safe }}` 본문 영역
   - `<script src="/js/copy.js" defer></script>` (Plan 03 대비, 파일 없어도 빌드 오류 없음)
   - 인라인 `<script>` — 모바일 토글 동작:
     ```html
     <script>
       document.querySelector('.docs-nav-toggle')?.addEventListener('click', function () {
         const sidebar = document.getElementById('docs-sidebar');
         const expanded = this.getAttribute('aria-expanded') === 'true';
         this.setAttribute('aria-expanded', String(!expanded));
         sidebar.classList.toggle('is-open');
       });
     </script>
     ```
   - `.docs-sidebar`에 `id="docs-sidebar"` 부여
   - 최소 사이드바/콘텐츠 인라인 스타일 (artux.css 로드 전 레이아웃 깨짐 방지):
     ```html
     <style>
       .docs-layout { display: flex; min-height: 100vh; }
       .docs-sidebar { width: 220px; flex-shrink: 0; padding: 1rem; background: #f8f9fa; }
       .docs-content { flex: 1; padding: 1.5rem; max-width: 860px; }
       .docs-nav-toggle { display: none; }
       @media (max-width: 768px) {
         .docs-layout { flex-direction: column; }
         .docs-nav-toggle { display: block; }
         .docs-sidebar { display: none; }
         .docs-sidebar.is-open { display: block; }
       }
     </style>
     ```

3. docs/index.md 생성 (홈 페이지):
   ```yaml
   ---
   layout: layouts/base.njk
   title: artpqUX 퍼블리싱 가이드
   ---
   ```
   본문: `# artpqUX 퍼블리싱 가이드` + 1~2문장 소개 ("아트피큐 팀 퍼블리싱 표준 시스템입니다.")
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Desktop/artpqUX && npx @11ty/eleventy --dryrun 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/_includes/layouts/base.njk 존재
    - grep "docs-sidebar\|docs-content\|docs-nav-toggle" docs/_includes/layouts/base.njk → 3개 매치
    - grep "aria-expanded\|aria-controls\|aria-label" docs/_includes/layouts/base.njk → 2개 이상 매치
    - docs/_data/navigation.js 존재, 8개 섹션 항목 포함
    - docs/index.md 존재, frontmatter에 layout: layouts/base.njk 포함
    - npx eleventy --dryrun 실행 시 에러 없이 완료
  </acceptance_criteria>
  <done>base.njk + navigation 데이터 + index.md 생성 완료, eleventy --dryrun 통과</done>
</task>

</tasks>

<verification>
npm run build 실행 → _site/index.html 생성 확인:
```
cd /Users/johyeonchang/Desktop/artpqUX && npm run build 2>&1 | tail -10
ls _site/index.html _site/artux.css 2>/dev/null
```

두 파일 모두 존재하면 Wave 1 완료.
</verification>

<success_criteria>
- npm run build 실행 후 _site/index.html 존재
- _site/artux.css 존재 (passthrough 복사)
- eleventy.config.js ESM import 정상
- package.json에 serve + build 스크립트 존재
</success_criteria>

<output>
완료 후 `.planning/phases/10-eleventy-문서-사이트/10-01-SUMMARY.md` 생성
</output>
