---
phase: 13-v1-0-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/_includes/layouts/base.njk
  - docs/_data/navigation.js
  - docs/starter/index.md
  - scss/7-utilities/_index.scss
autonomous: true
requirements: [SCSS-01, SCSS-02, SCSS-03, SCSS-04, SCSS-05, SCSS-06, SCSS-07, TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05, TOKEN-06, STARTER-04, DOCS-03]

must_haves:
  truths:
    - "REQUIREMENTS.md에서 SCSS-01~07, TOKEN-01~06이 [x]로 표시된다"
    - "SCSS-03 요구사항 텍스트에 focus()가 _focus.scss에 있다고 명시된다"
    - "문서 사이트에서 artux.css가 로드된다 (base.njk)"
    - "문서 사이트 네비게이션에 스타터 킷 섹션이 존재한다"
    - "docs/starter/index.md 파일이 존재하고 starter/README.md 내용을 담는다"
    - "7-utilities/_index.scss 주석이 실제 상태를 반영한다"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "13개 항목 Complete 상태"
      contains: "[x] **SCSS-01**"
    - path: "docs/_includes/layouts/base.njk"
      provides: "artux.css link 태그"
      contains: "artux.css"
    - path: "docs/_data/navigation.js"
      provides: "스타터 킷 섹션"
      contains: "스타터 킷"
    - path: "docs/starter/index.md"
      provides: "스타터 킷 문서 사이트 페이지"
      contains: "빠른 시작"
    - path: "scss/7-utilities/_index.scss"
      provides: "정확한 주석"
      contains: "sr-only는 4-elements"
  key_links:
    - from: "docs/_data/navigation.js"
      to: "docs/starter/index.md"
      via: "url: /starter/"
      pattern: "url.*starter"
---

<objective>
v1.0 마일스톤 감사에서 발견된 tech debt 5건을 일괄 정리한다.
코드 기능은 모두 완료 상태이며, 문서 정합성 복구 + 문서 사이트 minor gap 2건 + 코드 주석 수정 1건만 수행한다.

Purpose: v1.0 마일스톤 추적 문서와 실제 구현 상태의 정합성을 맞추고, 문서 사이트에서 스타터 킷 접근과 토큰 미리보기를 가능하게 한다.
Output: REQUIREMENTS.md (13항목 Complete), base.njk (artux.css 링크), navigation.js (스타터 킷 섹션), docs/starter/index.md (신규), _index.scss (주석 수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md 추적 테이블 업데이트 + SCSS-03 텍스트 수정</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Read `.planning/REQUIREMENTS.md` 전체 내용 확인 후 아래 두 가지 수정을 적용한다.

    **수정 1 — 13항목 Pending → Complete (per D-01):**
    SCSS 구조 섹션에서 SCSS-01 ~ SCSS-07 (7개), 디자인 토큰 섹션에서 TOKEN-01 ~ TOKEN-06 (6개)의
    체크박스를 `- [ ]`에서 `- [x]`로 변경한다.

    **수정 2 — SCSS-03 요구사항 텍스트 수정 (per D-02):**
    현재 텍스트:
    ```
    - [x] **SCSS-03**: 팀 공통 믹스인 파일(`_mixin.scss`)이 표준화된다 (focus, flex, ellipsis, position, drop-shadow, ani 등)
    ```
    변경 후 텍스트:
    ```
    - [x] **SCSS-03**: 팀 공통 믹스인 파일(`_mixin.scss`)이 표준화된다 (flex, ellipsis, position, drop-shadow, ani 등); 포커스 스타일은 `_focus.scss`로 분리됨
    ```
    (focus가 _mixin.scss → _focus.scss로 이동된 Phase 05 아키텍처 결정 반영)
  </action>
  <verify>
    grep -c '\- \[x\] \*\*SCSS-0' /Users/johyeonchang/Desktop/artpqUX/.planning/REQUIREMENTS.md
    # 출력: 7
    grep -c '\- \[x\] \*\*TOKEN-0' /Users/johyeonchang/Desktop/artpqUX/.planning/REQUIREMENTS.md
    # 출력: 6
    grep '_focus.scss' /Users/johyeonchang/Desktop/artpqUX/.planning/REQUIREMENTS.md
    # SCSS-03 줄에 _focus.scss가 포함되어야 함
  </verify>
  <done>
    SCSS-01~07 모두 [x], TOKEN-01~06 모두 [x], SCSS-03 텍스트에 `_focus.scss`로 분리됨 명시
  </done>
</task>

<task type="auto">
  <name>Task 2: 문서 사이트 gap 3건 + 주석 수정</name>
  <files>
    docs/_includes/layouts/base.njk,
    docs/_data/navigation.js,
    docs/starter/index.md,
    scss/7-utilities/_index.scss
  </files>
  <action>
    **수정 A — base.njk에 artux.css link 추가 (per D-03):**
    Read `docs/_includes/layouts/base.njk` 확인 후,
    `<link rel="stylesheet" href="/css/docs.css">` 바로 다음 줄에 추가:
    ```html
      <link rel="stylesheet" href="/css/artux.css">
    ```
    (docs.css 이후에 위치해야 artux.css가 docs.css를 오버라이드하지 않음)

    **수정 B — docs/starter/index.md 신규 생성 (per D-04):**
    `docs/starter/` 디렉토리를 생성하고 `docs/starter/index.md`를 아래 내용으로 작성한다.
    starter/README.md의 핵심 내용을 문서 사이트 형식으로 변환한다:

    ```markdown
    ---
    layout: base
    title: 스타터 킷
    ---

    # 스타터 킷

    아트피큐 팀 퍼블리싱 표준 환경 — 신규 프로젝트 시작 템플릿

    ## 빠른 시작 (5단계)

    1. **이 디렉토리를 새 프로젝트에 복사**
       ```
       cp -r starter/ /path/to/my-project/
       cd /path/to/my-project
       ```

    2. **의존성 설치**
       ```
       npm install
       ```

    3. **프로젝트 정보 수정** — `package.json`의 `name`, `description` 수정

    4. **CSS 빌드**
       ```
       npm run build:css
       ```
       → `dist/style.css` 생성

    5. **개발 시작** — `html/index.html`을 열고 작업 시작
       ```
       npm run watch:css
       ```

    ---

    ## SCSS 디렉토리 구조

    ITCSS(Inverted Triangle CSS) 기반 7단계 레이어:

    ```
    scss/
    ├── style.scss              # 진입점 — 로드 순서 정의
    ├── 1-settings/
    │   ├── _variables.scss     # Bootstrap 오버라이드 변수 (문서화 원본)
    │   └── _project-overrides.scss  # 프로젝트별 색상/토큰 오버라이드
    ├── 2-tools/
    │   ├── _mixin.scss         # 팀 믹스인 5종 (flex, ellipsis, position, drop-shadow, ani)
    │   └── _breakpoints.scss   # respond-to() 반응형 믹스인 (6키워드)
    ├── 3-generic/
    │   ├── _root.scss          # CSS Custom Properties 디자인 토큰
    │   └── _vendor.scss        # Bootstrap @import 격리
    ├── 4-elements/
    │   ├── _base.scss          # 62.5% REM, body font-size
    │   ├── _font.scss          # Pretendard GOV @font-face
    │   ├── _common.scss        # word-break, a, button, .sr-only
    │   └── _focus.scss         # 포커스 스타일 (KWCAG 2.1 AA)
    ├── 5-objects/
    │   └── _layout.scss        # Bootstrap 그리드 중복 금지 원칙
    ├── 6-components/           # 프로젝트별 UI 컴포넌트 추가
    └── 7-utilities/            # 프로젝트별 유틸리티 클래스 추가
    ```

    ---

    ## 프로젝트 색상 변경

    색상 변경 시 두 곳을 함께 수정해야 한다:

    **1. CSS Custom Properties** — `scss/3-generic/_root.scss`
    ```scss
    :root {
      --color-primary: #1a73e8;  // 변경
    }
    ```

    **2. Bootstrap Sass 변수** — `scss/3-generic/_vendor.scss`
    ```scss
    $primary: #1a73e8;  // 동일하게 변경
    ```

    또는 `scss/1-settings/_project-overrides.scss` 주석을 해제하여 사용:
    ```scss
    :root {
      --color-primary: #1a73e8;
      --bs-primary:    #1a73e8;
    }
    ```

    ---

    ## 반응형 브레이크포인트

    `respond-to()` 믹스인 사용:

    ```scss
    @use '../2-tools' as tools;

    .element {
      // 모바일 기본
      font-size: 1.4rem;

      // 태블릿 이상
      @include tools.respond-to(tablet) {
        font-size: 1.6rem;
      }

      // PC 이상
      @include tools.respond-to(pc) {
        font-size: 1.8rem;
      }
    }
    ```

    | 키워드 | 범위 |
    |--------|------|
    | `mobile-only` | ~767px |
    | `tablet` | 768px~ |
    | `tablet-only` | 768px~1023px |
    | `pc-sm` | 1024px~ |
    | `pc-sm-only` | 1024px~1279px |
    | `pc` | 1280px~ |

    ---

    ## 폰트 설정

    **공공기관 프로젝트 (기본):** Pretendard GOV — `scss/4-elements/_font.scss` 활성화
    → 폰트 파일(`font/woff2/*.woff2`, `font/woff/*.woff`)을 프로젝트에 포함해야 한다.
    → 다운로드: https://github.com/orioncactus/pretendard/releases

    **일반 프로젝트:** Noto Sans KR CDN 방식으로 전환
    → `_font.scss` 주석 지침에 따라 HTML `<link>` 태그 방식으로 교체

    ---

    ## 접근성 기준

    이 스타터 킷은 KWCAG 2.1 AA 준수를 기본으로 한다:
    - 본문건너뛰기 링크 포함 (`html/index.html`)
    - 포커스 인디케이터 표준 적용 (`scss/4-elements/_focus.scss`)
    - 스크린리더 전용 텍스트 유틸리티 `.sr-only` 내장
    - `word-break: keep-all` 한국어 줄바꿈 처리
    ```

    **수정 C — navigation.js에 스타터 킷 섹션 추가 (per D-05):**
    Read `docs/_data/navigation.js` 확인 후, 배열 마지막 항목(컴포넌트 섹션) 뒤, 닫는 `]` 앞에 추가:

    ```js
      {
        section: "스타터 킷",
        items: [
          { title: "스타터 킷", url: "/starter/" },
        ]
      },
    ```

    **수정 D — 7-utilities/_index.scss 주석 수정 (per D-06):**
    Read `scss/7-utilities/_index.scss` 확인 후, 4번째 줄 주석을 수정:

    현재:
    ```scss
    // Phase 5에서 채워짐
    ```
    변경:
    ```scss
    // 현재 비어 있음 — sr-only는 4-elements/_common.scss에 위치
    // 프로젝트별 유틸리티 클래스를 이 레이어에 추가
    ```
  </action>
  <verify>
    grep 'artux.css' /Users/johyeonchang/Desktop/artpqUX/docs/_includes/layouts/base.njk
    # 출력: <link rel="stylesheet" href="/css/artux.css">

    ls /Users/johyeonchang/Desktop/artpqUX/docs/starter/index.md
    # 파일 존재 확인

    grep '스타터 킷' /Users/johyeonchang/Desktop/artpqUX/docs/_data/navigation.js
    # 출력: section: "스타터 킷" 포함 줄

    grep 'sr-only는 4-elements' /Users/johyeonchang/Desktop/artpqUX/scss/7-utilities/_index.scss
    # 출력: sr-only는 4-elements/_common.scss에 위치 포함 줄
  </verify>
  <done>
    base.njk에 artux.css link 존재, docs/starter/index.md 생성됨, navigation.js에 스타터 킷 섹션 포함, _index.scss 주석이 실제 상태를 반영
  </done>
</task>

</tasks>

<verification>
모든 수정 완료 후 일괄 확인:

```bash
# 1. REQUIREMENTS.md — SCSS/TOKEN 항목 Complete 상태
grep -E '^\- \[x\] \*\*(SCSS|TOKEN)-' /Users/johyeonchang/Desktop/artpqUX/.planning/REQUIREMENTS.md | wc -l
# 출력: 13

# 2. SCSS-03 _focus.scss 언급 확인
grep 'SCSS-03' /Users/johyeonchang/Desktop/artpqUX/.planning/REQUIREMENTS.md

# 3. base.njk artux.css 로드 확인
grep 'artux.css' /Users/johyeonchang/Desktop/artpqUX/docs/_includes/layouts/base.njk

# 4. 스타터 킷 페이지 프론트매터 확인
head -5 /Users/johyeonchang/Desktop/artpqUX/docs/starter/index.md

# 5. navigation.js 스타터 킷 섹션 확인
grep -A3 '스타터 킷' /Users/johyeonchang/Desktop/artpqUX/docs/_data/navigation.js

# 6. _index.scss 주석 확인
cat /Users/johyeonchang/Desktop/artpqUX/scss/7-utilities/_index.scss
```
</verification>

<success_criteria>
- REQUIREMENTS.md: SCSS-01~07, TOKEN-01~06 13개 항목 모두 [x]
- REQUIREMENTS.md: SCSS-03 텍스트에 `_focus.scss로 분리됨` 명시
- base.njk: `<link rel="stylesheet" href="/css/artux.css">` 존재 (docs.css 다음 줄)
- docs/starter/index.md: layout: base 프론트매터 + 빠른 시작 내용 포함
- navigation.js: `{ section: "스타터 킷", ... }` 섹션 추가됨
- scss/7-utilities/_index.scss: `Phase 5에서 채워짐` 주석 제거, 실제 상태 반영 주석으로 교체
</success_criteria>

<output>
완료 후 `.planning/phases/13-v1-0-tech-debt/13-01-SUMMARY.md`를 생성한다.
</output>
